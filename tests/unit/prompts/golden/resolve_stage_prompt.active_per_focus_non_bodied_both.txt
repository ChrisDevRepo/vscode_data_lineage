### Capture recipe for THIS focus node (write each key to its target field)

- structural_summary: Fires only when the focus node has no DDL body (table/non-bodied). For this hop only, it replaces `business_capture` and `technical_capture`. It replaces the recipes but not the section count: submit one section per angle the mission requires, each carrying this same body, because the locked classification decides how many sections a hop must contain and the focus type does not change it.
Describe what this node **is**; point to the surrounding procedures for what it **does**. Non-bodied nodes carry structure, not behaviour.
⛔ STRICT GROUNDING — use only `focus_node` data. Use only columns, objects, neighbors, and relationships present in that data. If a field is missing/empty, output the explicit empty-case text below.
Required parts, in this order, each opened with a bold label on its own line:
**Purpose** One short sentence naming the role of this node in the pipeline. Source: node id / type from focus_node.
**Columns** From `cols[]` only, render a `| Column | Type | Role / ⚠️ |` table. Role is a short phrase (key / dim / metric / audit / computed). Flag nullable columns with ⚠️. If `cols[]` is absent: write "Column metadata not available in current model."
**Upstream sources** Populate from `in[]` in focus_node — these are the nodes that write TO this table. One line per entry: node id + one-sentence role. If `in[]` is empty or absent: write "No upstream sources found in graph."
**Downstream consumers** Populate from `out[]` in focus_node — these are the nodes that read FROM this table. One line per entry: node id + one-sentence role. If `out[]` is empty or absent: write "No downstream consumers found in graph."
**Grain / keys** Derive from PK-flagged columns in `cols[]` and `fks[]` in focus_node only. If neither is present: write "Key information not available in current model."
Scope: structure and relationships only — the surrounding procedure slots carry all transform logic.