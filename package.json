{
  "name": "data-lineage-viz",
  "displayName": "Data Lineage Viz",
  "preview": true,
  "description": "Visualize object dependencies and data lineage from .dacpac files or by importing from SQL Server, Azure SQL, Fabric DW, and Synapse databases",
  "version": "0.9.9",
  "publisher": "datahelper-chwagner",
  "license": "MIT",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Data Science",
    "Visualization",
    "Other"
  ],
  "keywords": [
    "dacpac",
    "sql",
    "lineage",
    "database",
    "dag",
    "visualization",
    "dependency",
    "schema",
    "stored-procedure",
    "sql-server",
    "azure-sql",
    "fabric",
    "synapse",
    "database-project",
    "data-lineage"
  ],
  "icon": "images/icon.png",
  "galleryBanner": {
    "color": "#1e1e1e",
    "theme": "dark"
  },
  "badges": [
    {
      "url": "https://img.shields.io/badge/license-MIT-blue.svg",
      "href": "https://github.com/ChrisDevRepo/vscode_data_lineage/blob/main/LICENSE",
      "description": "License: MIT"
    }
  ],
  "bugs": {
    "url": "https://github.com/ChrisDevRepo/vscode_data_lineage/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ChrisDevRepo/vscode_data_lineage"
  },
  "homepage": "https://chrisdevrepo.github.io/vscode_data_lineage/",
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": true
    }
  },
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "dacpac-sql",
        "aliases": [
          "DACPAC SQL"
        ]
      }
    ],
    "grammars": [
      {
        "language": "dacpac-sql",
        "scopeName": "source.dacpac-sql",
        "path": "./syntaxes/dacpac-sql.json"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "dataLineageViz",
          "title": "Data Lineage",
          "icon": "images/icon.svg"
        }
      ]
    },
    "views": {
      "dataLineageViz": [
        {
          "id": "dataLineageViz.quickActions",
          "name": "Quick Actions",
          "icon": "$(graph)",
          "contextualTitle": "Data Lineage"
        }
      ]
    },
    "commands": [
      {
        "command": "dataLineageViz.open",
        "title": "Data Lineage: Open Wizard",
        "icon": "$(graph)"
      },
      {
        "command": "dataLineageViz.openDemo",
        "title": "Data Lineage: Open Demo",
        "icon": "$(play)"
      },
      {
        "command": "dataLineageViz.openSettings",
        "title": "Data Lineage: Settings",
        "icon": "$(gear)"
      },
      {
        "command": "dataLineageViz.createParseRules",
        "title": "Data Lineage: Create Parse Rules",
        "icon": "$(file-code)"
      },
      {
        "command": "dataLineageViz.createDmvQueries",
        "title": "Data Lineage: Create DMV Queries",
        "icon": "$(database)"
      },
      {
        "command": "dataLineageViz.createAiOutputTemplates",
        "title": "Data Lineage: Create AI Output Templates",
        "icon": "$(sparkle)"
      },
      {
        "command": "dataLineageViz.toggleOverviewMode",
        "title": "Data Lineage: Toggle Schema Overview Mode",
        "icon": "$(graph)"
      },
      {
        "command": "dataLineageViz.aiCreateView",
        "title": "Data Lineage: Show AI Trace in Graph",
        "icon": "$(type-hierarchy-sub)"
      },
      {
        "command": "dataLineageViz.copyDebugInfo",
        "title": "Data Lineage: Copy Debug Info",
        "icon": "$(bug)"
      },
      {
        "command": "dataLineageViz.dumpSmState",
        "title": "Data Lineage: Dump SM State"
      }
    ],
    "keybindings": [],
    "menus": {
      "view/title": [
        {
          "command": "dataLineageViz.open",
          "when": "view == dataLineageViz.quickActions",
          "group": "navigation@1"
        }
      ]
    },
    "configuration": [
      {
        "title": "Import",
        "properties": {
          "dataLineageViz.maxNodes": {
            "type": "number",
            "default": 750,
            "minimum": 10,
            "maximum": 10000,
            "markdownDescription": "Maximum number of database objects to load. The graph rendering is separately capped by `renderLimit`. Values above 1000 are supported for AI/data queries but the GUI will show schema overview or a limit warning.",
            "scope": "resource"
          },
          "dataLineageViz.renderLimit": {
            "type": "number",
            "default": 750,
            "minimum": 100,
            "maximum": 5000,
            "markdownDescription": "Maximum rendered nodes before showing a warning instead of the graph. Prevents browser performance issues with very large visualizations. AI tools and BFS operate on the full model regardless of this limit.",
            "scope": "resource"
          },
          "dataLineageViz.excludePatterns": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "markdownDescription": "Regex patterns to permanently exclude objects by name at load time (e.g. `_TEST$`, `^staging\\\\.`). Matched against `schema.object`. Supports `%` wildcard. Requires reloading the data source to take effect. For interactive ad-hoc filtering without reload, use the Exclusion Rules toolbar button.",
            "scope": "resource"
          },
          "dataLineageViz.externalRefs.enabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Detect external references (OPENROWSET file paths, cross-database 3-part names). When disabled, no virtual external nodes are created. External Tables are unaffected.",
            "scope": "resource"
          },
          "dataLineageViz.overview.enabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Enable schema overview mode. When enabled and the graph exceeds `overview.threshold` nodes, the initial view shows schema-level bubbles instead of all objects. Set to `false` to always show the full object graph.",
            "scope": "resource"
          },
          "dataLineageViz.overview.threshold": {
            "type": "number",
            "default": 150,
            "minimum": 10,
            "maximum": 1000,
            "markdownDescription": "Node count above which schema overview mode auto-activates on initial load. Only applies when `overview.enabled` is `true`.",
            "scope": "resource"
          },
          "dataLineageViz.parseRulesFile": {
            "type": "string",
            "default": "",
            "markdownDescription": "Path to a custom parse rules YAML file. Leave empty for built-in defaults. [Customization guide](https://github.com/ChrisDevRepo/vscode_data_lineage/blob/main/docs/PARSE_RULES.md)",
            "scope": "resource"
          }
        }
      },
      {
        "title": "Database Connection",
        "properties": {
          "dataLineageViz.dmvQueryTimeout": {
            "type": "number",
            "default": 120,
            "minimum": 10,
            "maximum": 600,
            "markdownDescription": "Timeout (seconds) per DMV query. Increase for large databases.",
            "scope": "resource"
          },
          "dataLineageViz.dmvQueriesFile": {
            "type": "string",
            "default": "",
            "markdownDescription": "Path to a custom DMV queries YAML file. Leave empty for built-in defaults. [Customization guide](https://github.com/ChrisDevRepo/vscode_data_lineage/blob/main/docs/DMV_QUERIES.md)",
            "scope": "resource"
          }
        }
      },
      {
        "title": "Table Statistics",
        "properties": {
          "dataLineageViz.tableStatistics.enabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Show column statistics and row counts in the table design viewer (database import only).",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.standardModeEnabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Allow **Standard** profiling mode (adds AVG, STDEV, zero/empty counts). When `false`, only Quick mode is available — lighter queries.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.excludeExternalTables": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Skip profiling for external tables. External tables query remote data sources (S3, Blob, other databases) — profiling can be slow and expensive.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.queryTimeout": {
            "type": "number",
            "default": 60,
            "minimum": 10,
            "maximum": 600,
            "markdownDescription": "Timeout (seconds) per profiling query.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.sampleThreshold": {
            "type": "number",
            "default": 100000,
            "minimum": 0,
            "maximum": 999999999,
            "markdownDescription": "Advanced: Tables above this row count use sampling instead of a full scan. Set to `0` to always sample.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.sampleSize": {
            "type": "number",
            "default": 10000,
            "minimum": 100,
            "maximum": 1000000,
            "markdownDescription": "Advanced: Number of rows to sample when profiling large tables.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.useApproxDistinct": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Advanced: Use `APPROX_COUNT_DISTINCT` instead of exact `COUNT(DISTINCT)`. Much faster with ~2% margin. Requires SQL Server 2019+.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.maxColumns": {
            "type": "number",
            "default": 50,
            "minimum": 1,
            "maximum": 500,
            "markdownDescription": "Advanced: Maximum number of columns to profile per table. Columns beyond this cap are skipped. Prevents massive queries on wide tables.",
            "scope": "resource"
          }
        }
      },
      {
        "title": "Layout",
        "properties": {
          "dataLineageViz.layout.direction": {
            "type": "string",
            "enum": [
              "TB",
              "LR"
            ],
            "default": "LR",
            "enumDescriptions": [
              "Top to Bottom",
              "Left to Right (default)"
            ],
            "markdownDescription": "Direction of the dependency graph layout.",
            "scope": "resource"
          },
          "dataLineageViz.layout.edgeStyle": {
            "type": "string",
            "enum": [
              "default",
              "smoothstep",
              "step",
              "straight"
            ],
            "default": "default",
            "enumDescriptions": [
              "Smooth bezier curves",
              "Smooth step edges with rounded corners",
              "Step edges with sharp corners",
              "Straight lines"
            ],
            "markdownDescription": "Visual style for dependency edges.",
            "scope": "resource"
          },
          "dataLineageViz.layout.edgeAnimation": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Animate edges when running a trace.",
            "scope": "resource"
          },
          "dataLineageViz.layout.highlightAnimation": {
            "type": "boolean",
            "default": false,
            "markdownDescription": "Animate edges when clicking a node.",
            "scope": "resource"
          },
          "dataLineageViz.layout.minimapEnabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Show the graph minimap for navigation.",
            "scope": "resource"
          },
          "dataLineageViz.layout.rankSeparation": {
            "type": "number",
            "default": 120,
            "minimum": 20,
            "maximum": 300,
            "markdownDescription": "Advanced: Horizontal spacing between dependency layers (pixels).",
            "scope": "resource"
          },
          "dataLineageViz.layout.nodeSeparation": {
            "type": "number",
            "default": 30,
            "minimum": 10,
            "maximum": 200,
            "markdownDescription": "Advanced: Vertical spacing between nodes in the same layer (pixels).",
            "scope": "resource"
          }
        }
      },
      {
        "title": "Trace",
        "properties": {
          "dataLineageViz.trace.defaultUpstreamLevels": {
            "type": "number",
            "default": 3,
            "minimum": 0,
            "maximum": 99,
            "markdownDescription": "How many levels upstream (inputs) to trace by default.",
            "scope": "resource"
          },
          "dataLineageViz.trace.defaultDownstreamLevels": {
            "type": "number",
            "default": 3,
            "minimum": 0,
            "maximum": 99,
            "markdownDescription": "How many levels downstream (outputs) to trace by default.",
            "scope": "resource"
          }
        }
      },
      {
        "title": "Analysis",
        "properties": {
          "dataLineageViz.analysis.hubMinDegree": {
            "type": "number",
            "default": 8,
            "minimum": 1,
            "maximum": 50,
            "markdownDescription": "Minimum number of connections for an object to appear in the hub list.",
            "scope": "resource"
          },
          "dataLineageViz.analysis.islandMaxSize": {
            "type": "number",
            "default": 500,
            "minimum": 2,
            "maximum": 1000,
            "markdownDescription": "Max connected component size to qualify as an island. Higher values include larger disconnected subgraphs; set lower (e.g. 5) to focus on small isolated pairs/groups only.",
            "scope": "resource"
          },
          "dataLineageViz.analysis.longestPathMinNodes": {
            "type": "number",
            "default": 5,
            "minimum": 2,
            "maximum": 50,
            "markdownDescription": "Minimum number of objects in a dependency chain for longest-path analysis.",
            "scope": "resource"
          }
        }
      },
      {
        "title": "AI Assistant",
        "properties": {
          "dataLineageViz.ai.enabled": {
            "type": "boolean",
            "default": true,
            "description": "Enable the @lineage chat participant and AI lineage tools. Disable for users who do not use VS Code Copilot or want to remove AI features entirely."
          },
          "dataLineageViz.ai.maxRounds": {
            "type": "integer",
            "default": 50,
            "minimum": 5,
            "maximum": 100,
            "markdownDescription": "Maximum tool-call rounds per `@lineage` request. Increase for complex explorations on deep graphs. Each round is one AI reasoning step + tool call."
          },
          "dataLineageViz.ai.inlineTokenBudget": {
            "type": "integer",
            "default": 10000,
            "minimum": 1000,
            "maximum": 100000,
            "markdownDescription": "Maximum estimated tokens for inline (quick) delivery. When a trace or exploration scope fits within **both** this token budget and the node cap, the AI receives all SQL at once for faster analysis. Exceeding either threshold switches to hop-by-hop deep exploration with persistent memory. Lower values favor accuracy; higher values favor speed."
          },
          "dataLineageViz.ai.inlineNodeCap": {
            "type": "integer",
            "default": 10,
            "minimum": 1,
            "maximum": 100,
            "markdownDescription": "Maximum scope nodes for inline (quick) delivery. When a trace or exploration scope has more nodes than this, the AI explores hop-by-hop with persistent memory for deeper analysis. Deep mode tracks column renames and transformations across many dependency stages. Lower values favor accuracy; higher values favor speed."
          },
          "dataLineageViz.ai.outputTemplateFile": {
            "type": "string",
            "default": "",
            "markdownDescription": "Path to custom AI output templates YAML. Controls how `@lineage` formats summary, description, badges, highlights, and notes. Leave empty for built-in defaults. Use Command Palette → *Create AI Output Templates* to scaffold. [Customization guide](https://github.com/ChrisDevRepo/vscode_data_lineage/blob/main/docs/AI_PROMPTS.md)"
          },
          "dataLineageViz.ai.showToolInvocations": {
            "type": "boolean",
            "default": false,
            "markdownDescription": "Show each `@lineage` tool call as an expandable chat part (with input JSON). When off (default), the copy button captures only the AI prose; tool progress is shown ephemerally via status text and full tool I/O is available in the **Data Lineage Viz** output channel. Turn on for developer debugging — note that VS Code's built-in chat copy will then include tool-call details."
          }
        }
      }
    ],
    "chatParticipants": [
      {
        "id": "dataLineageViz.lineage",
        "name": "lineage",
        "fullName": "Data Lineage Assistant",
        "description": "Ask questions about your loaded lineage graph — tables, procedures, dependencies, data flow.",
        "isSticky": true,
        "commands": [
          {
            "name": "trace",
            "description": "Trace data lineage for an object or column"
          },
          {
            "name": "search",
            "description": "Search for database objects by name or schema"
          }
        ],
        "disambiguation": [
          {
            "category": "lineage",
            "description": "Questions about database dependencies, data flow, stored procedures, table lineage, SQL object relationships, where data comes from or goes to",
            "examples": [
              "Where does revenue come from",
              "Trace the data flow for orders",
              "What tables does this procedure read from",
              "Show me the lineage for the financehub schema",
              "What are the upstream sources of this table"
            ]
          }
        ]
      }
    ],
    "languageModelTools": [
      {
        "name": "lineage_get_context",
        "toolReferenceName": "lineage_get_context",
        "displayName": "Get Lineage Context",
        "userDescription": "Shows what is currently loaded and visible in the lineage graph.",
        "modelDescription": "Returns schemas, stats, active filter, saved views. If objects[] present: full dataset with DDL, columns, FKs, edges — answer directly. Otherwise: use schema names in other tools' schemas[] filters.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage",
          "lineage-research"
        ],
        "icon": "$(info)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "lineage_search_objects",
        "toolReferenceName": "lineage_search_objects",
        "displayName": "Search Objects",
        "userDescription": "Search for database objects by name.",
        "modelDescription": "Search objects by name or column name (substring or mode='regex' for multi-pattern). Returns IDs for BFS/detail. Results with match='column' matched a column inside the object, not the object name. Use schemas[] to narrow scope. For DDL body content search, use search_ddl instead. If 0 results: try search_ddl for DDL body matches before telling the user nothing was found. If 0 results with schemas[]: check schema_mismatch field — object may exist in another schema.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage",
          "lineage-research"
        ],
        "icon": "$(search)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Name to search for (max 200 chars)"
            },
            "types": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "table",
                  "view",
                  "procedure",
                  "function",
                  "external"
                ]
              },
              "description": "Include only these object types. Omit for all."
            },
            "schemas": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Include only these schemas. Omit for all."
            },
            "mode": {
              "type": "string",
              "enum": [
                "substring",
                "regex"
              ],
              "description": "substring (default) or regex for multi-pattern ('rev|gl.*cog')"
            }
          },
          "required": [
            "query"
          ]
        }
      },
      {
        "name": "lineage_get_object_detail",
        "toolReferenceName": "lineage_get_object_detail",
        "displayName": "Get Object Detail",
        "userDescription": "Get full details for a specific database object.",
        "modelDescription": "Full metadata for ONE object: columns, FKs, neighbors (up/dn), DDL. Use for single-object questions ('what does spX do?'). For multi-object lineage, use run_bfs_trace instead.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage",
          "lineage-research"
        ],
        "icon": "$(symbol-class)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Exact object ID from lineage_search_objects (e.g. '[dbo].[FactSales]')"
            }
          },
          "required": [
            "id"
          ]
        }
      },
      {
        "name": "lineage_run_bfs_trace",
        "toolReferenceName": "lineage_run_bfs_trace",
        "displayName": "Trace Lineage",
        "userDescription": "Trace data lineage upstream and/or downstream from an object.",
        "modelDescription": "Scope discovery tool — use to understand graph size and shape before deciding on approach. Two modes:\n- Level mode (default): id + upstream_hops + downstream_hops → explore by depth\n- Path mode: id + target → all nodes on paths between start and end\nReturns nodes with DDL, columns, and edges. BFS is for discovery only; for annotated output (enrich_view), follow BFS with start_exploration — enrich_view requires a completed state-machine result.\nUse schemas[] on large models.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage"
        ],
        "icon": "$(type-hierarchy-sub)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Exact object ID to trace from"
            },
            "upstream_hops": {
              "type": "number",
              "description": "Upstream depth (0 = none). Default 3."
            },
            "downstream_hops": {
              "type": "number",
              "description": "Downstream depth (0 = none). Default 3."
            },
            "types": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "table",
                  "view",
                  "procedure",
                  "function",
                  "external"
                ]
              },
              "description": "Include only these node types. Omit for all."
            },
            "schemas": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Include only nodes in these schemas. Omit for all."
            },
            "include_ddl": {
              "type": "boolean",
              "description": "Include DDL/columns in results (default true). Set false for structure-only overview."
            },
            "target": {
              "type": "string",
              "description": "Path mode: find all nodes on paths from id to target. When provided, upstream_hops/downstream_hops are ignored."
            }
          },
          "required": [
            "id"
          ]
        }
      },
      {
        "name": "lineage_run_analysis",
        "toolReferenceName": "lineage_run_analysis",
        "displayName": "Run Graph Analysis",
        "userDescription": "Run structural analysis on the loaded lineage graph.",
        "modelDescription": "Graph-wide structural analysis. Use for: 'most connected objects' or 'biggest nodes' (type=hubs), 'isolated tables with no dependencies' (type=orphans), 'deepest dependency chains' or 'longest paths' (type=longest-path), 'circular dependencies' (type=cycles), 'disconnected groups' (type=islands), 'external or cross-database references' (type=external-refs). Returns ranked groups of node IDs with summary.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage",
          "lineage-research"
        ],
        "icon": "$(graph)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "hubs",
                "islands",
                "orphans",
                "longest-path",
                "cycles",
                "external-refs"
              ]
            },
            "min_degree": {
              "type": "number",
              "description": "Hubs only: minimum degree to qualify as hub. Default from config."
            },
            "max_size": {
              "type": "number",
              "description": "Islands only: maximum component size to include. Default from config."
            }
          },
          "required": [
            "type"
          ]
        }
      },
      {
        "name": "lineage_search_ddl",
        "toolReferenceName": "lineage_search_ddl",
        "displayName": "Search DDL",
        "userDescription": "Search SQL body scripts for a text pattern.",
        "modelDescription": "Regex search across SP/view/function DDL bodies. Returns matching lines with context. Use for pattern-based DDL search; for simple keyword name search use search_objects instead.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage",
          "lineage-research"
        ],
        "icon": "$(file-code)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Text or regex to search in DDL body scripts (max 200 chars)"
            },
            "types": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "view",
                  "procedure",
                  "function"
                ]
              },
              "description": "Limit search to these object types. Default: all DDL-bearing types."
            }
          },
          "required": [
            "query"
          ]
        }
      },
      {
        "name": "lineage_enrich_view",
        "toolReferenceName": "lineage_enrich_view",
        "displayName": "Enrich View",
        "userDescription": "Enrich a stored result graph with a name, summary, optional description, highlight groups, and text annotations. The graph comes from a completed state-machine exploration — you provide the presentation layer.",
        "modelDescription": "Enrich the stored result graph from your last exploration. Requires a completed state-machine result; call start_exploration first (BFS alone is not accepted). Provide name and summary. Emit one sections[] entry per archived detail_slot: label = slot.badge_label verbatim, node_ids = [slot.nodeId], text = slot.analysis expanded with business interpretation. Adjust labels only to fix typos or group clear duplicates — keep variant siblings as separate sections. notes[] is complementary (hover/caption per node), not a replacement for sections. System numbers labels in your written order; reference other groups by label name, never by number. Use prune_node_ids to remove irrelevant nodes. Write each section.text and the description fallback at the depth the archive supports — no length limit, no compression; synthesis is the one place to lift every finding forward. BAD: notes-only without sections, numbered label ('1 Source'), one section for 20 variant siblings. GOOD: { label: 'Transform Variant', node_ids: ['[schema].[spAllocRuleA]'], text: 'Full breakdown with SQL quoted verbatim, LaTeX formulas and column I/O table' }",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage"
        ],
        "icon": "$(sparkle)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "maxLength": 60,
              "description": "Display name for the view (max 60 chars)"
            },
            "summary": {
              "type": "string",
              "maxLength": 300,
              "description": "One-line graph purpose for the info card. This is the card headline, not the analysis — the per-node text inside sections[] is where depth goes."
            },
            "title": {
              "type": "string",
              "maxLength": 80,
              "description": "Optional. Doc heading (≤80 chars) rendered as # at the top of the description panel. Name the analysis subject and its key finding."
            },
            "intro": {
              "type": "string",
              "description": "Optional. 2–4 sentence paragraph rendered BEFORE the numbered ## sections."
            },
            "closing": {
              "type": "string",
              "maxLength": 400,
              "description": "Optional. 1–2 sentences rendered AFTER the sections behind a --- divider."
            },
            "prune_node_ids": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Node IDs to remove from the stored result graph."
            },
            "layout_direction": {
              "type": "string",
              "enum": [
                "LR",
                "TB"
              ],
              "description": "Graph layout direction — LR for sequential flows, TB for hierarchies (default TB)"
            },
            "highlight_groups": {
              "type": "array",
              "description": "Glow on 2-3 critical nodes only.",
              "items": {
                "type": "object",
                "properties": {
                  "label": {
                    "type": "string",
                    "description": "Short group label"
                  },
                  "color": {
                    "type": "string",
                    "enum": [
                      "source",
                      "transform",
                      "target",
                      "good",
                      "warn",
                      "fail"
                    ]
                  },
                  "node_ids": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "Node IDs in this group"
                  }
                },
                "required": [
                  "label",
                  "color",
                  "node_ids"
                ]
              }
            },
            "sections": {
              "type": "array",
              "description": "Document sections — one entry per logical group.",
              "items": {
                "type": "object",
                "properties": {
                  "label": {
                    "type": "string",
                    "description": "Section title and badge chip text."
                  },
                  "node_ids": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "Nodes associated with this section."
                  },
                  "text": {
                    "type": "string",
                    "description": "Markdown description for this section. Detail findings go here."
                  }
                },
                "required": [
                  "label",
                  "text"
                ]
              }
            },
            "notes": {
              "type": "array",
              "description": "Annotations below nodes.",
              "items": {
                "type": "object",
                "properties": {
                  "node_id": {
                    "type": "string"
                  },
                  "text": {
                    "type": "string",
                    "maxLength": 400,
                    "description": "First line = title. Rest = hover detail."
                  }
                },
                "required": [
                  "node_id",
                  "text"
                ]
              }
            },
            "is_update": {
              "type": "boolean",
              "description": "Set true to patch an existing view instead of creating a new one."
            }
          },
          "required": [
            "name",
            "summary"
          ]
        }
      },
      {
        "name": "lineage_get_ddl_batch",
        "toolReferenceName": "lineage_get_ddl_batch",
        "displayName": "Get DDL for Multiple Objects",
        "userDescription": "Get DDL body scripts for multiple objects in one call.",
        "modelDescription": "Batch DDL fetch for known IDs. Use ONLY for objects not already in a BFS result — BFS includes DDL by default.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage"
        ],
        "icon": "$(file-code)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "ids": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Exact object IDs to fetch DDL for (max 20)."
            }
          },
          "required": [
            "ids"
          ]
        }
      },
      {
        "name": "lineage_start_exploration",
        "toolReferenceName": "lineage_start_exploration",
        "when": "dataLineageViz.modelLoaded",
        "displayName": "Start Exploration",
        "userDescription": "Start an autonomous exploration of database objects for data flow, business rules, or investigations.",
        "modelDescription": "Start a hop-by-hop exploration across database objects. Use for both broad investigations (Blackboard) and specific column-level tracking. If targetColumns provided, the engine constrains navigation to relevant column paths. If question only, it runs free-form Blackboard exploration.\n\nDEPTH HANDLING (three modes — pick the one that matches how the user expressed scope):\n- `depth_enforcement: 'strict'` — only when the user typed an explicit slash-command depth. Routes beyond the budget are hard-rejected.\n- `depth_enforcement: 'soft'` — when the user expressed depth in natural language ('1 level deep', 'direct neighbors', 'immediate dependencies'). Engine allows expansion but surfaces a `depth_budget` + `in_budget: false` flags so you can prefer staying within.\n- `depth_enforcement: 'silent'` (default) — when the user did NOT specify depth. On large graphs prefer starting shallow (e.g. `depth: 2`) and let the engine auto-expand as your route_requests need more reach. No user-facing constraint; no awareness fields in the hop payload — explore freely.\n\nTypical choice: no explicit depth in the question → `depth_enforcement: 'silent'` with a small `depth` (1–2) on wide graphs. User said '1 level deep' → `'soft'` with `depth: 1`.\n\nReturns: first hop context with Topological Map, current Incremental Blackboard, Focus DDL, and Neighbor Metadata (Columns).\n\nUse Selection-Inference Routing: read metadata and generate a sub-question for each relevant neighbor.",
        "tags": [
          "lineage",
          "lineage-engine"
        ],
        "inputSchema": {
          "type": "object",
          "properties": {
            "question": {
              "type": "string",
              "description": "The user's exploration question"
            },
            "origin": {
              "type": "string",
              "description": "Starting node ID. Engine runs depth-limited BFS from here to seed the exploration agenda."
            },
            "targetColumns": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Optional. Specific column names to trace (activates Column Trace mode)."
            },
            "direction": {
              "type": "string",
              "enum": [
                "upstream",
                "downstream",
                "bidirectional"
              ],
              "description": "BFS direction for initial scope discovery."
            },
            "depth": {
              "type": "number",
              "description": "Initial BFS depth from origin (default 5). On large graphs prefer a small value (1–2) with depth_enforcement='silent' to start cautious and auto-expand."
            },
            "depth_enforcement": {
              "type": "string",
              "enum": ["strict", "soft", "silent"],
              "description": "How to treat the depth. 'strict' = hard boundary (slash-command-set); 'soft' = advisory (user said it in chat); 'silent' = cautious-start / auto-expand (no user constraint — default)."
            },
            "schemas": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Optional: override active filter to search specific schemas."
            }
          },
          "required": [
            "question",
            "origin"
          ]
        }
      },
      {
        "name": "lineage_submit_findings",
        "toolReferenceName": "lineage_submit_findings",
        "when": "dataLineageViz.modelLoaded",
        "displayName": "Submit Findings",
        "userDescription": "Submit analysis of the current node and propose next routes in the exploration.",
        "modelDescription": "Submit analysis of the current exploration hop and receive the next hop's data.\n\nFields:\n- detail_analysis: Full technical write-up for this node — business purpose, transform expressions, column I/O, relationships, risks. The archive is the sole evidence used at synthesis; it is unbounded and preserved verbatim. Write thoroughly — thin analysis produces thin synthesis.\n- summary: Specific one-line digest for this hop. Carries forward to future hops as context.\n- verdict: relevant | pass | irrelevant.\n    - 'relevant' → stores findings, drives badges/notes.\n    - 'pass' → visited, no analysis stored, always accepted. Use for variant siblings of an already-analyzed archetype.\n    - 'irrelevant' → cascade-prunes (utility/logging/generic helpers). May be rejected by orphan/cascade guards; fall back to 'pass'.\n\nLabels (required for 'relevant' and 'pass' — the enriched view has no badges/notes without them):\n- badge_label: 2-4 word semantic role tag (Source / Transform / Transform Variant / Staging / Output / Validation / Aggregation, or a domain-specific role the DDL suggests).\n- note_caption: One-line what-this-does caption shown on the graph node.\n\nRouting:\n- route_requests: Array of { nodeId, question, columns? }. Every neighbor entry carries a focused sub-question — single yes/no or multi-part investigation, whichever matches the depth the next hop needs. `columns` is honored only in column_trace mode. The engine validates every request against neighbor metadata.",
        "tags": [
          "lineage",
          "lineage-engine"
        ],
        "inputSchema": {
          "type": "object",
          "properties": {
            "focus_node_id": {
              "type": "string",
              "description": "ID of the current focus node."
            },
            "detail_analysis": {
              "type": "string",
              "description": "Full per-node analysis. Archive is unbounded — write at the depth the node's complexity warrants; quote SQL verbatim."
            },
            "summary": {
              "type": "string",
              "description": "Specific one-line digest for this hop."
            },
            "verdict": {
              "type": "string",
              "enum": [
                "relevant",
                "pass",
                "irrelevant"
              ]
            },
            "route_requests": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "nodeId": {
                    "type": "string",
                    "description": "Exact neighbor ID to visit."
                  },
                  "question": {
                    "type": "string",
                    "description": "Focused sub-question for this neighbor — single yes/no or multi-part investigation, whichever depth the next hop needs."
                  },
                  "columns": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "For column tracking: specific input columns to follow."
                  }
                },
                "required": [
                  "nodeId",
                  "question"
                ]
              },
              "description": "Grounded routing requests for next hops."
            },
            "complete": {
              "type": "boolean",
              "description": "Set true on your final submit when every direct neighbor of the origin has been analyzed. The engine validates: if any direct neighbor is still unvisited it returns 'complete_rejected' with the list, promotes them to priority 3, and serves them next. Retry 'complete: true' after analyzing them."
            },
            "badge_label": {
              "type": "string",
              "description": "Short semantic role (2-4 words) for enriched view badge."
            },
            "note_caption": {
              "type": "string",
              "description": "One-line caption for enriched view note."
            }
          },
          "required": [
            "focus_node_id",
            "detail_analysis",
            "summary",
            "verdict"
          ]
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run build:ext && npm run build:webview",
    "build": "npm run build:ext && npm run build:webview",
    "build:ext": "node esbuild.config.mjs",
    "build:webview": "vite build",
    "dev": "vite",
    "watch": "npm run watch:ext",
    "watch:ext": "node esbuild.config.mjs --watch",
    "test": "tsx tests/unit/dacpacExtractor.test.ts && tsx tests/unit/graphBuilder.test.ts && tsx tests/unit/parser-edge-cases.test.ts && tsx tests/unit/graphAnalysis.test.ts && tsx tests/unit/graph-analysis-aw.test.ts && tsx tests/unit/dmvExtractor.test.ts && tsx tests/unit/tsql-complex.test.ts && tsx tests/unit/projectStore.test.ts && tsx tests/unit/snapshot-aw-baseline.ts && tsx tests/unit/ai-tool-registration.test.ts && tsx tests/unit/sm-robustness.test.ts",
    "test:unit": "npm run test",
    "test:unit:ai": "tsx tests/unit/ai-tools.test.ts && tsx tests/unit/navigation-engine.test.ts && tsx tests/unit/navigation-engine-cascade.test.ts",
    "test:dacpac": "tsx tests/unit/dacpacExtractor.test.ts",
    "test:graph": "tsx tests/unit/graphBuilder.test.ts",
    "test:parser": "tsx tests/unit/parser-edge-cases.test.ts",
    "test:analysis": "tsx tests/unit/graphAnalysis.test.ts && tsx tests/unit/graph-analysis-aw.test.ts",
    "test:dmv": "tsx tests/unit/dmvExtractor.test.ts",
    "test:sql": "tsx tests/unit/tsql-complex.test.ts",
    "test:project": "tsx tests/unit/projectStore.test.ts",
    "test:integration": "tsx tests/integration/integration-db.test.ts",
    "test:e2e": "npm run build:ext && node esbuild.test.mjs && vscode-test --files out/test/tests/e2e/suite/extension.test.js",
    "test:eval": "npm run build:ext && node esbuild.test.mjs && vscode-test --files out/test/tests/e2e/suite/eval/eval.test.js",
    "test:hooks": "vitest run --config vitest.config.ts",
    "test:snapshot": "tsx tests/unit/snapshot-aw-baseline.ts",
    "test:snapshot:update": "tsx tests/unit/snapshot-aw-baseline.ts --update",
    "test:eval:server": "tsx test-internal/ai-test-server.ts",
    "package": "npx @vscode/vsce package"
  },
  "dependencies": {
    "@dagrejs/dagre": "^2.0.4",
    "@floating-ui/react": "^0.27.19",
    "@monaco-editor/react": "^4.7.0",
    "@xyflow/react": "^12.4.0",
    "fast-xml-parser": "^5.5.9",
    "graphology": "^0.25.4",
    "graphology-components": "^1.5.4",
    "graphology-shortest-path": "^2.1.0",
    "graphology-traversal": "^0.3.1",
    "graphology-types": "^0.24.8",
    "js-yaml": "^4.1.0",
    "jszip": "^3.10.1",
    "monaco-editor": "^0.53.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-markdown": "^10.1.0",
    "rehype-katex": "^7.0.1",
    "remark-gfm": "^4.0.1",
    "remark-math": "^6.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@types/assert": "^1.5.11",
    "@types/js-yaml": "^4.0.9",
    "@types/jsdom": "^28.0.1",
    "@types/mocha": "^10.0.10",
    "@types/mssql": "^9",
    "@types/node": "^20.11.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@types/vscode": "^1.95.0",
    "@vitejs/plugin-react": "^4.3.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "autoprefixer": "^10.4.20",
    "dotenv": "^16",
    "esbuild": "^0.25.0",
    "glob": "^13.0.6",
    "jsdom": "^29.0.1",
    "mocha": "^11.3.0",
    "mssql": "^11",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vite": "^6.4.2",
    "vitest": "^3.2.4"
  },
  "overrides": {
    "dompurify": "^3.4.0",
    "serialize-javascript": "^7.0.5",
    "picomatch": "4.0.4"
  }
}
