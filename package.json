{
  "name": "data-lineage-viz",
  "displayName": "Data Lineage Viz",
  "preview": true,
  "description": "Visualize object dependencies and data lineage from .dacpac files or by importing from SQL Server, Azure SQL, Fabric DW, and Synapse databases",
  "version": "0.9.6",
  "publisher": "datahelper-chwagner",
  "license": "MIT",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Data Science",
    "Visualization",
    "Other"
  ],
  "keywords": [
    "dacpac",
    "sql",
    "lineage",
    "database",
    "dag",
    "visualization",
    "dependency",
    "schema",
    "stored-procedure",
    "sql-server",
    "azure-sql",
    "fabric",
    "synapse",
    "database-project",
    "data-lineage"
  ],
  "icon": "images/icon.png",
  "galleryBanner": {
    "color": "#1e1e1e",
    "theme": "dark"
  },
  "badges": [
    {
      "url": "https://img.shields.io/badge/license-MIT-blue.svg",
      "href": "https://github.com/ChrisDevRepo/vscode_data_lineage/blob/main/LICENSE",
      "description": "License: MIT"
    }
  ],
  "bugs": {
    "url": "https://github.com/ChrisDevRepo/vscode_data_lineage/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ChrisDevRepo/vscode_data_lineage"
  },
  "homepage": "https://chrisdevrepo.github.io/vscode_data_lineage/",
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": true
    }
  },
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "dacpac-sql",
        "aliases": [
          "DACPAC SQL"
        ]
      }
    ],
    "grammars": [
      {
        "language": "dacpac-sql",
        "scopeName": "source.dacpac-sql",
        "path": "./syntaxes/dacpac-sql.json"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "dataLineageViz",
          "title": "Data Lineage",
          "icon": "images/icon.svg"
        }
      ]
    },
    "views": {
      "dataLineageViz": [
        {
          "id": "dataLineageViz.quickActions",
          "name": "Quick Actions",
          "icon": "$(graph)",
          "contextualTitle": "Data Lineage"
        }
      ]
    },
    "commands": [
      {
        "command": "dataLineageViz.open",
        "title": "Data Lineage: Open Wizard",
        "icon": "$(graph)"
      },
      {
        "command": "dataLineageViz.openDemo",
        "title": "Data Lineage: Open Demo",
        "icon": "$(play)"
      },
      {
        "command": "dataLineageViz.openSettings",
        "title": "Data Lineage: Settings",
        "icon": "$(gear)"
      },
      {
        "command": "dataLineageViz.createParseRules",
        "title": "Data Lineage: Create Parse Rules",
        "icon": "$(file-code)"
      },
      {
        "command": "dataLineageViz.createDmvQueries",
        "title": "Data Lineage: Create DMV Queries",
        "icon": "$(database)"
      },
      {
        "command": "dataLineageViz.toggleOverviewMode",
        "title": "Data Lineage: Toggle Schema Overview Mode",
        "icon": "$(graph)"
      }
    ],
    "keybindings": [],
    "menus": {
      "view/title": [
        {
          "command": "dataLineageViz.open",
          "when": "view == dataLineageViz.quickActions",
          "group": "navigation@1"
        }
      ]
    },
    "configuration": [
      {
        "title": "Import",
        "properties": {
          "dataLineageViz.maxNodes": {
            "type": "number",
            "default": 750,
            "minimum": 10,
            "maximum": 10000,
            "markdownDescription": "Maximum number of database objects to load. The graph rendering is separately capped by `renderLimit`. Values above 1000 are supported for AI/data queries but the GUI will show schema overview or a limit warning.",
            "scope": "resource"
          },
          "dataLineageViz.renderLimit": {
            "type": "number",
            "default": 750,
            "minimum": 100,
            "maximum": 5000,
            "markdownDescription": "Maximum rendered nodes before showing a warning instead of the graph. Prevents browser performance issues with very large visualizations. AI tools and BFS operate on the full model regardless of this limit.",
            "scope": "resource"
          },
          "dataLineageViz.excludePatterns": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "markdownDescription": "Regex patterns to permanently exclude objects by name at load time (e.g. `_TEST$`, `^staging\\\\.`). Matched against `schema.object`. Supports `%` wildcard. Requires reloading the data source to take effect. For interactive ad-hoc filtering without reload, use the Exclusion Rules toolbar button.",
            "scope": "resource"
          },
          "dataLineageViz.externalRefs.enabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Detect external references (OPENROWSET file paths, cross-database 3-part names). When disabled, no virtual external nodes are created. External Tables are unaffected.",
            "scope": "resource"
          },
          "dataLineageViz.overview.enabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Enable schema overview mode. When enabled and the graph exceeds `overview.threshold` nodes, the initial view shows schema-level bubbles instead of all objects. Set to `false` to always show the full object graph.",
            "scope": "resource"
          },
          "dataLineageViz.overview.threshold": {
            "type": "number",
            "default": 150,
            "minimum": 10,
            "maximum": 1000,
            "markdownDescription": "Node count above which schema overview mode auto-activates on initial load. Only applies when `overview.enabled` is `true`.",
            "scope": "resource"
          },
          "dataLineageViz.overview.forceOverviewThreshold": {
            "type": "number",
            "default": 300,
            "minimum": 10,
            "maximum": 2000,
            "markdownDescription": "Node count above which overview mode is forced even after manual toggle (soft guard). Prevents accidentally rendering very large graphs. Must be >= `overview.threshold`.",
            "scope": "resource"
          },
          "dataLineageViz.parseRulesFile": {
            "type": "string",
            "default": "",
            "markdownDescription": "Path to a custom parse rules YAML file. Leave empty for built-in defaults. [Customization guide](https://github.com/ChrisDevRepo/vscode_data_lineage/blob/main/docs/PARSE_RULES.md)",
            "scope": "resource"
          }
        }
      },
      {
        "title": "Database Connection",
        "properties": {
          "dataLineageViz.dmvQueryTimeout": {
            "type": "number",
            "default": 120,
            "minimum": 10,
            "maximum": 600,
            "markdownDescription": "Timeout (seconds) per DMV query. Increase for large databases.",
            "scope": "resource"
          },
          "dataLineageViz.dmvQueriesFile": {
            "type": "string",
            "default": "",
            "markdownDescription": "Path to a custom DMV queries YAML file. Leave empty for built-in defaults. [Customization guide](https://github.com/ChrisDevRepo/vscode_data_lineage/blob/main/docs/DMV_QUERIES.md)",
            "scope": "resource"
          }
        }
      },
      {
        "title": "Table Statistics",
        "properties": {
          "dataLineageViz.tableStatistics.enabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Show column statistics and row counts in the table design viewer (database import only).",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.standardModeEnabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Allow **Standard** profiling mode (adds AVG, STDEV, zero/empty counts). When `false`, only Quick mode is available — lighter queries.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.excludeExternalTables": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Skip profiling for external tables. External tables query remote data sources (S3, Blob, other databases) — profiling can be slow and expensive.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.queryTimeout": {
            "type": "number",
            "default": 60,
            "minimum": 10,
            "maximum": 600,
            "markdownDescription": "Timeout (seconds) per profiling query.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.sampleThreshold": {
            "type": "number",
            "default": 100000,
            "minimum": 0,
            "maximum": 999999999,
            "markdownDescription": "Advanced: Tables above this row count use sampling instead of a full scan. Set to `0` to always sample.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.sampleSize": {
            "type": "number",
            "default": 10000,
            "minimum": 100,
            "maximum": 1000000,
            "markdownDescription": "Advanced: Number of rows to sample when profiling large tables.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.useApproxDistinct": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Advanced: Use `APPROX_COUNT_DISTINCT` instead of exact `COUNT(DISTINCT)`. Much faster with ~2% margin. Requires SQL Server 2019+.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.maxColumns": {
            "type": "number",
            "default": 50,
            "minimum": 1,
            "maximum": 500,
            "markdownDescription": "Advanced: Maximum number of columns to profile per table. Columns beyond this cap are skipped. Prevents massive queries on wide tables.",
            "scope": "resource"
          }
        }
      },
      {
        "title": "Layout",
        "properties": {
          "dataLineageViz.layout.direction": {
            "type": "string",
            "enum": [
              "TB",
              "LR"
            ],
            "default": "LR",
            "enumDescriptions": [
              "Top to Bottom",
              "Left to Right (default)"
            ],
            "markdownDescription": "Direction of the dependency graph layout.",
            "scope": "resource"
          },
          "dataLineageViz.layout.edgeStyle": {
            "type": "string",
            "enum": [
              "default",
              "smoothstep",
              "step",
              "straight"
            ],
            "default": "default",
            "enumDescriptions": [
              "Smooth bezier curves",
              "Smooth step edges with rounded corners",
              "Step edges with sharp corners",
              "Straight lines"
            ],
            "markdownDescription": "Visual style for dependency edges.",
            "scope": "resource"
          },
          "dataLineageViz.layout.edgeAnimation": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Animate edges when running a trace.",
            "scope": "resource"
          },
          "dataLineageViz.layout.highlightAnimation": {
            "type": "boolean",
            "default": false,
            "markdownDescription": "Animate edges when clicking a node.",
            "scope": "resource"
          },
          "dataLineageViz.layout.minimapEnabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Show the graph minimap for navigation.",
            "scope": "resource"
          },
          "dataLineageViz.layout.rankSeparation": {
            "type": "number",
            "default": 120,
            "minimum": 20,
            "maximum": 300,
            "markdownDescription": "Advanced: Horizontal spacing between dependency layers (pixels).",
            "scope": "resource"
          },
          "dataLineageViz.layout.nodeSeparation": {
            "type": "number",
            "default": 30,
            "minimum": 10,
            "maximum": 200,
            "markdownDescription": "Advanced: Vertical spacing between nodes in the same layer (pixels).",
            "scope": "resource"
          }
        }
      },
      {
        "title": "Trace",
        "properties": {
          "dataLineageViz.trace.defaultUpstreamLevels": {
            "type": "number",
            "default": 3,
            "minimum": 0,
            "maximum": 99,
            "markdownDescription": "How many levels upstream (inputs) to trace by default.",
            "scope": "resource"
          },
          "dataLineageViz.trace.defaultDownstreamLevels": {
            "type": "number",
            "default": 3,
            "minimum": 0,
            "maximum": 99,
            "markdownDescription": "How many levels downstream (outputs) to trace by default.",
            "scope": "resource"
          }
        }
      },
      {
        "title": "Analysis",
        "properties": {
          "dataLineageViz.analysis.hubMinDegree": {
            "type": "number",
            "default": 8,
            "minimum": 1,
            "maximum": 50,
            "markdownDescription": "Minimum number of connections for an object to appear in the hub list.",
            "scope": "resource"
          },
          "dataLineageViz.analysis.islandMaxSize": {
            "type": "number",
            "default": 500,
            "minimum": 2,
            "maximum": 1000,
            "markdownDescription": "Max connected component size to qualify as an island. Default (500) matches `maxNodes` — shows all disconnected subgraphs. Set lower (e.g. 5) to focus on small isolated pairs/groups only.",
            "scope": "resource"
          },
          "dataLineageViz.analysis.longestPathMinNodes": {
            "type": "number",
            "default": 5,
            "minimum": 2,
            "maximum": 50,
            "markdownDescription": "Minimum number of objects in a dependency chain for longest-path analysis.",
            "scope": "resource"
          }
        }
      },
      {
        "title": "AI Assistant",
        "properties": {
          "dataLineageViz.ai.enabled": {
            "type": "boolean",
            "default": true,
            "description": "Enable the @lineage chat participant and AI lineage tools. Disable for users who do not use VS Code Copilot or want to remove AI features entirely."
          },
          "dataLineageViz.ai.searchMaxResults": {
            "type": "integer",
            "default": 50,
            "minimum": 10,
            "maximum": 500,
            "markdownDescription": "Maximum results per `@lineage` object or DDL search. Auto-scales with the selected model's context window when not explicitly set. Raise for large projects with high-capacity models (Claude max tier, strong local LLMs)."
          },
          "dataLineageViz.ai.bfsMaxNodes": {
            "type": "integer",
            "default": 200,
            "minimum": 50,
            "maximum": 750,
            "markdownDescription": "Maximum nodes returned by `@lineage` BFS trace queries. Auto-scales with the selected model's context window when not explicitly set."
          },
          "dataLineageViz.ai.bfsMaxEdges": {
            "type": "integer",
            "default": 300,
            "minimum": 50,
            "maximum": 2000,
            "markdownDescription": "Maximum edges returned by `@lineage` BFS trace queries. Auto-scales with the selected model's context window when not explicitly set."
          },
          "dataLineageViz.ai.analysisMaxGroups": {
            "type": "integer",
            "default": 100,
            "minimum": 20,
            "maximum": 500,
            "markdownDescription": "Maximum groups returned by `@lineage` analysis queries (hubs, islands, orphans, cycles). Auto-scales with the selected model's context window when not explicitly set."
          },
          "dataLineageViz.ai.maxDdlChars": {
            "type": "integer",
            "default": 10000,
            "minimum": 1000,
            "maximum": 500000,
            "markdownDescription": "Maximum DDL body characters per object (per single `getObjectDetail` call). When exceeded, an error is returned instead of partial DDL. Synapse/DWH stored procedures are typically 15,000–80,000 chars — raise to `500000` to effectively disable the limit. Auto-scales to `500000` when the selected model's context exceeds 128K tokens (e.g. Claude Sonnet)."
          }
        }
      }
    ],
    "chatParticipants": [
      {
        "id": "dataLineageViz.lineage",
        "name": "lineage",
        "fullName": "Data Lineage Assistant",
        "description": "Ask questions about your loaded lineage graph — tables, procedures, dependencies, data flow.",
        "isSticky": true
      }
    ],
    "languageModelTools": [
      {
        "name": "lineage_get_context",
        "toolReferenceName": "lineage_get_context",
        "displayName": "Get Lineage Context",
        "userDescription": "Shows what is currently loaded and visible in the lineage graph.",
        "modelDescription": "Start every new conversation by calling this tool. Returns project name, db_platform, model stats (nodes/edges/schemas), visible node count, filter (active schemas/types/searchTerm/bookmark_nodes count — allowlist IDs not included), and saved views list.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage"
        ],
        "icon": "$(info)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "lineage_get_schema_summary",
        "toolReferenceName": "lineage_get_schema_summary",
        "displayName": "Get Schema Summary",
        "userDescription": "Lists all schemas and object counts in the loaded database.",
        "modelDescription": "Returns all schemas with per-type object counts (tables/views/procedures/functions/external) for the full model — ignores the current UI filter. Call after lineage_get_context when you need the complete schema breakdown. Use schema names from this response as filter inputs to other tools.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage"
        ],
        "icon": "$(database)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "lineage_search_objects",
        "toolReferenceName": "lineage_search_objects",
        "displayName": "Search Objects",
        "userDescription": "Search for database objects by name.",
        "modelDescription": "Search ALL database objects by name (case-insensitive substring or regex). Results include id, schema (s), name (n), type (t), degree (deg=total connections), match source. Set include_body:true to also search DDL bodies. Use schemas/types to include only specific schemas/types; use exclude_schemas/exclude_types to post-filter results. Pass IDs verbatim to other tools.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage"
        ],
        "icon": "$(search)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Name to search for (substring or regex, max 200 chars)"
            },
            "types": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "table",
                  "view",
                  "procedure",
                  "function",
                  "external"
                ]
              },
              "description": "Filter to specific object types. Omit for all types."
            },
            "schemas": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Filter to specific schema names. Omit to search all schemas."
            },
            "external_subtypes": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "et",
                  "file",
                  "db"
                ]
              },
              "description": "When types includes 'external': filter by subtype. Omit for all."
            },
            "include_body": {
              "type": "boolean",
              "description": "Also search DDL body scripts (views, procedures, functions). Body matches include a snippet and match:'body'. Omit or false for name-only search."
            },
            "exclude_schemas": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Remove results from matching schemas. Use % as wildcard (matches any sequence). Exact names also work. E.g. 'test%' excludes all schemas starting with 'test', '%staging%' excludes any schema containing 'staging', 'test_data' matches only 'test_data'."
            },
            "exclude_types": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "table",
                  "view",
                  "procedure",
                  "function",
                  "external"
                ]
              },
              "description": "Remove results of these object types."
            }
          },
          "required": [
            "query"
          ]
        }
      },
      {
        "name": "lineage_get_object_detail",
        "toolReferenceName": "lineage_get_object_detail",
        "displayName": "Get Object Detail",
        "userDescription": "Get full details for a specific database object.",
        "modelDescription": "Returns full metadata for ONE object: schema, columns (n/t/nl/pk/uq/ck), foreign keys, inline neighbors (up/dn arrays), and full DDL. Use for single-object questions ('what columns does X have?', 'show me the DDL of X'). For multi-object dependency analysis use lineage_run_bfs_trace instead.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage"
        ],
        "icon": "$(symbol-class)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Exact object ID from lineage_search_objects (e.g. '[dbo].[FactSales]')"
            }
          },
          "required": [
            "id"
          ]
        }
      },
      {
        "name": "lineage_run_bfs_trace",
        "toolReferenceName": "lineage_run_bfs_trace",
        "displayName": "Trace Lineage",
        "userDescription": "Trace data lineage upstream and/or downstream from an object.",
        "modelDescription": "BFS trace from an object. By default (include_ddl:true) returns DDL for procedures/views/functions and column lists for tables — use for code review and dependency analysis. Set include_ddl:false for structure-only (hop counts, path shape, no DDL loaded). Include filters (schemas/types) are allowlists; exclude filters (exclude_schemas/exclude_types) are post-filters. Response includes excluded_count when exclusions remove nodes. exclude_schemas uses % as wildcard (e.g. 'test%', '%staging%'); other characters including _ are literal.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage"
        ],
        "icon": "$(type-hierarchy-sub)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Exact object ID to trace from (from lineage_search_objects)"
            },
            "upstream_hops": {
              "type": "number",
              "description": "Upstream depth (0 = no upstream). Default: 3"
            },
            "downstream_hops": {
              "type": "number",
              "description": "Downstream depth (0 = no downstream). Default: 3"
            },
            "include_ddl": {
              "type": "boolean",
              "description": "Include DDL/columns in results (default true). Set false for structure-only — returns node list + edges, no DDL loaded."
            },
            "types": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "table",
                  "view",
                  "procedure",
                  "function",
                  "external"
                ]
              },
              "description": "Include only these node types. Omit for all."
            },
            "schemas": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Include only nodes in these schemas (exact names from lineage_get_schema_summary). Omit for all."
            },
            "exclude_schemas": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Remove nodes in matching schemas. Use % as wildcard (matches any sequence); other chars including _ are literal. E.g. 'test%' removes all schemas starting with 'test', '%staging%' removes any schema containing 'staging'."
            },
            "exclude_types": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "table",
                  "view",
                  "procedure",
                  "function",
                  "external"
                ]
              },
              "description": "Remove nodes of these types from results."
            }
          },
          "required": [
            "id"
          ]
        }
      },
      {
        "name": "lineage_run_analysis",
        "toolReferenceName": "lineage_run_analysis",
        "displayName": "Run Graph Analysis",
        "userDescription": "Run structural analysis on the loaded lineage graph.",
        "modelDescription": "Runs a structural analysis on the full graph. Types: 'hubs' (high-degree connector objects), 'islands' (isolated subgraphs), 'orphans' (objects with no connections), 'longest-path' (deepest dependency chains), 'cycles' (circular dependencies), 'external-refs' (external tables and file/DB virtual nodes). Returns groups of node IDs with summary.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage"
        ],
        "icon": "$(graph)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "hubs",
                "islands",
                "orphans",
                "longest-path",
                "cycles",
                "external-refs"
              ]
            },
            "min_degree": {
              "type": "number",
              "description": "Hubs only: minimum degree to qualify as hub. Default from config."
            },
            "max_size": {
              "type": "number",
              "description": "Islands only: maximum component size to include. Default from config."
            }
          },
          "required": [
            "type"
          ]
        }
      },
      {
        "name": "lineage_search_ddl",
        "toolReferenceName": "lineage_search_ddl",
        "displayName": "Search DDL",
        "userDescription": "Search SQL body scripts for a text pattern.",
        "modelDescription": "Full-text search across all loaded SP/view/function body scripts using a substring or regex. Returns matching object IDs and the matching line(s) with context. Use for 'find all procedures that reference column Amount' or 'find objects using deprecated function Y'.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage"
        ],
        "icon": "$(file-code)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Text or regex to search in DDL body scripts (max 200 chars)"
            },
            "types": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "view",
                  "procedure",
                  "function"
                ]
              },
              "description": "Limit search to these object types. Default: all DDL-bearing types."
            }
          },
          "required": [
            "query"
          ]
        }
      },
      {
        "name": "lineage_create_ai_view",
        "toolReferenceName": "lineage_create_ai_view",
        "displayName": "Create AI View",
        "userDescription": "Create an AI-authored advanced bookmark with a curated set of nodes, narrative explanation, color groups, and badges. The user receives a VS Code notification and sees the view applied in the lineage graph.",
        "modelDescription": "Saves a named bookmark and applies it immediately in the graph. Use for BOTH simple bookmarks (name + node_ids only) and rich AI views (add narrative, highlight_groups, badges). Requires an active project. Node IDs are validated before saving. Always activates the view and notifies the user — only use when the user wants to save or visualize a result.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage"
        ],
        "icon": "$(sparkle)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "maxLength": 60,
              "description": "Display name for the view (max 60 chars)"
            },
            "node_ids": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Exact node IDs to show (1–200, validated against catalog)"
            },
            "narrative": {
              "type": "string",
              "maxLength": 500,
              "description": "Plain text explanation of why these nodes matter (max 500 chars)"
            },
            "layout_direction": {
              "type": "string",
              "enum": [
                "LR",
                "TB"
              ],
              "description": "Graph layout direction — LR for sequential flows, TB for hierarchies (default TB)"
            },
            "highlight_groups": {
              "type": "array",
              "description": "Color groups for visual clustering (max 5)",
              "items": {
                "type": "object",
                "properties": {
                  "label": {
                    "type": "string",
                    "description": "Short group label"
                  },
                  "color": {
                    "type": "string",
                    "enum": [
                      "bu",
                      "gn",
                      "rd",
                      "ye",
                      "or"
                    ]
                  },
                  "node_ids": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "Node IDs in this group (must be subset of top-level node_ids)"
                  }
                },
                "required": [
                  "label",
                  "color",
                  "node_ids"
                ]
              }
            },
            "badges": {
              "type": "array",
              "description": "Short text chips shown on specific nodes (max 50)",
              "items": {
                "type": "object",
                "properties": {
                  "node_id": {
                    "type": "string"
                  },
                  "text": {
                    "type": "string",
                    "maxLength": 15,
                    "description": "Badge text (max 15 chars)"
                  },
                  "color": {
                    "type": "string",
                    "enum": [
                      "bu",
                      "gn",
                      "rd",
                      "ye",
                      "or",
                      "gy"
                    ]
                  }
                },
                "required": [
                  "node_id",
                  "text"
                ]
              }
            }
          },
          "required": [
            "name",
            "node_ids"
          ]
        }
      },
      {
        "name": "lineage_get_ddl_batch",
        "toolReferenceName": "lineage_get_ddl_batch",
        "displayName": "Get DDL for Multiple Objects",
        "userDescription": "Get DDL body scripts for multiple objects in one call.",
        "modelDescription": "Fetch DDL for up to 20 specific object IDs in one call. Use when you have known IDs and need their DDL without BFS traversal. Each result: {id, t, ddl} for scriptable objects, {id, t} for tables (no body), {id, error:'not_found'} for unknown IDs. ddl_too_large:true when DDL exceeds cap. Max 20 IDs — split if needed.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage"
        ],
        "icon": "$(file-code)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "ids": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Exact object IDs to fetch DDL for (max 20). Use IDs from lineage_run_bfs_trace or lineage_search_objects."
            }
          },
          "required": [
            "ids"
          ]
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run build:ext && npm run build:webview",
    "build": "npm run build:ext && npm run build:webview",
    "build:ext": "node esbuild.config.mjs",
    "build:webview": "vite build",
    "dev": "vite",
    "watch": "npm run watch:ext",
    "watch:ext": "node esbuild.config.mjs --watch",
    "test": "tsx test/dacpacExtractor.test.ts && tsx test/graphBuilder.test.ts && tsx test/parser-edge-cases.test.ts && tsx test/graphAnalysis.test.ts && tsx test/dmvExtractor.test.ts && tsx test/tsql-complex.test.ts && tsx test/projectStore.test.ts && tsx test/ai-tools.test.ts && tsx test/snapshot-aw-baseline.ts && vitest run --config vitest.config.ts",
    "test:dacpac": "tsx test/dacpacExtractor.test.ts",
    "test:graph": "tsx test/graphBuilder.test.ts",
    "test:parser": "tsx test/parser-edge-cases.test.ts",
    "test:analysis": "tsx test/graphAnalysis.test.ts",
    "test:dmv": "tsx test/dmvExtractor.test.ts",
    "test:sql": "tsx test/tsql-complex.test.ts",
    "test:project": "tsx test/projectStore.test.ts",
    "test:ai": "tsx test/ai-tools.test.ts",
    "test:snapshot": "tsx test/snapshot-aw-baseline.ts",
    "test:snapshot:update": "tsx test/snapshot-aw-baseline.ts --update",
    "test:hooks": "vitest run --config vitest.config.ts",
    "test:hooks:watch": "vitest --config vitest.config.ts",
    "test:coverage": "vitest run --config vitest.config.ts --coverage",
    "test:db": "tsx test-internal/integration-db.test.ts",
    "package": "npx @vscode/vsce package"
  },
  "dependencies": {
    "@dagrejs/dagre": "^2.0.4",
    "@floating-ui/react": "^0.27.19",
    "@monaco-editor/react": "^4.7.0",
    "@xyflow/react": "^12.4.0",
    "fast-xml-parser": "^5.5.9",
    "graphology": "^0.25.4",
    "graphology-components": "^1.5.4",
    "graphology-shortest-path": "^2.1.0",
    "graphology-traversal": "^0.3.1",
    "graphology-types": "^0.24.8",
    "js-yaml": "^4.1.0",
    "jszip": "^3.10.1",
    "monaco-editor": "^0.55.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@types/js-yaml": "^4.0.9",
    "@types/jsdom": "^28.0.1",
    "@types/mssql": "^9",
    "@types/node": "^20.11.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@types/vscode": "^1.95.0",
    "@vitejs/plugin-react": "^4.3.0",
    "autoprefixer": "^10.4.20",
    "dotenv": "^16",
    "esbuild": "^0.25.0",
    "jsdom": "^29.0.1",
    "mssql": "^11",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.2.4"
  },
  "overrides": {
    "dompurify": "3.3.3",
    "picomatch": "4.0.4"
  }
}
