{
  "name": "data-lineage-viz",
  "displayName": "Data Lineage Viz",
  "preview": false,
  "description": "Visualize object dependencies and data lineage from .dacpac files or by importing from SQL Server, Azure SQL, Fabric DW, and Synapse databases",
  "version": "1.0.0",
  "publisher": "datahelper-chwagner",
  "license": "MIT",
  "pricing": "Free",
  "markdown": "github",
  "qna": "marketplace",
  "extensionDependencies": [],
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Data Science",
    "Visualization",
    "Other"
  ],
  "keywords": [
    "dacpac",
    "sql",
    "lineage",
    "database",
    "dag",
    "visualization",
    "dependency",
    "schema",
    "stored-procedure",
    "sql-server",
    "azure-sql",
    "fabric",
    "synapse",
    "database-project",
    "data-lineage"
  ],
  "icon": "images/icon.png",
  "galleryBanner": {
    "color": "#1e1e1e",
    "theme": "dark"
  },
  "badges": [
    {
      "url": "https://img.shields.io/badge/license-MIT-blue.svg",
      "href": "https://github.com/ChrisDevRepo/vscode_data_lineage/blob/main/LICENSE",
      "description": "License: MIT"
    }
  ],
  "bugs": {
    "url": "https://github.com/ChrisDevRepo/vscode_data_lineage/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ChrisDevRepo/vscode_data_lineage"
  },
  "homepage": "https://datalineage.chwagner.eu/",
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": true
    }
  },
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "dacpac-sql",
        "aliases": [
          "DACPAC SQL"
        ]
      }
    ],
    "grammars": [
      {
        "language": "dacpac-sql",
        "scopeName": "source.dacpac-sql",
        "path": "./syntaxes/dacpac-sql.json"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "dataLineageViz",
          "title": "Data Lineage",
          "icon": "images/icon.svg"
        }
      ]
    },
    "views": {
      "dataLineageViz": [
        {
          "id": "dataLineageViz.quickActions",
          "name": "Quick Actions",
          "icon": "$(graph)",
          "contextualTitle": "Data Lineage"
        }
      ]
    },
    "commands": [
      {
        "command": "dataLineageViz.open",
        "title": "Data Lineage: Open Wizard",
        "icon": "$(graph)"
      },
      {
        "command": "dataLineageViz.openDemo",
        "title": "Data Lineage: Open Demo",
        "icon": "$(play)"
      },
      {
        "command": "dataLineageViz.openSettings",
        "title": "Data Lineage: Settings",
        "icon": "$(gear)"
      },
      {
        "command": "dataLineageViz.createParseRules",
        "title": "Data Lineage: Create Parse Rules",
        "icon": "$(file-code)"
      },
      {
        "command": "dataLineageViz.createDmvQueries",
        "title": "Data Lineage: Create DMV Queries",
        "icon": "$(database)"
      },
      {
        "command": "dataLineageViz.createAiOutputTemplates",
        "title": "Data Lineage: Create AI Output Templates",
        "icon": "$(sparkle)"
      },
      {
        "command": "dataLineageViz.aiCreateView",
        "title": "Data Lineage: Show AI Trace in Graph",
        "icon": "$(type-hierarchy-sub)"
      },
      {
        "command": "dataLineageViz.copyDebugInfo",
        "title": "Data Lineage: Copy Debug Info",
        "icon": "$(bug)"
      },
      {
        "command": "dataLineageViz.aiResolveGate",
        "title": "Data Lineage: Resolve AI Gate"
      },
      {
        "command": "dataLineageViz.dumpSmState",
        "title": "Data Lineage: Dump SM State"
      },
      {
        "command": "dataLineageViz.searchObjects",
        "title": "Data Lineage: Search Objects",
        "icon": "$(search)"
      }
    ],
    "keybindings": [],
    "menus": {
      "view/title": [
        {
          "command": "dataLineageViz.open",
          "when": "view == dataLineageViz.quickActions",
          "group": "navigation@1"
        }
      ]
    },
    "configuration": [
      {
        "title": "Import",
        "properties": {
          "dataLineageViz.maxNodes": {
            "type": "number",
            "default": 750,
            "minimum": 10,
            "maximum": 10000,
            "markdownDescription": "Maximum number of database objects to load. The graph rendering is separately capped by `renderLimit`. Values above 1000 are supported for AI/data queries but the GUI will show schema overview or a limit warning.",
            "scope": "resource"
          },
          "dataLineageViz.renderLimit": {
            "type": "number",
            "default": 750,
            "minimum": 100,
            "maximum": 5000,
            "markdownDescription": "Maximum rendered nodes before showing a warning instead of the graph. Prevents browser performance issues with very large visualizations. AI tools and BFS operate on the full model regardless of this limit.",
            "scope": "resource"
          },
          "dataLineageViz.excludePatterns": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "markdownDescription": "Regex patterns to permanently exclude objects by name at load time (e.g. `_TEST$`, `^staging\\\\.`). Matched against `schema.object`. Supports `%` wildcard. Requires reloading the data source to take effect. For interactive ad-hoc filtering without reload, use the Exclusion Rules toolbar button.",
            "scope": "resource"
          },
          "dataLineageViz.externalRefs.enabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Detect external references (OPENROWSET file paths, cross-database 3-part names). When disabled, no virtual external nodes are created. External Tables are unaffected.",
            "scope": "resource"
          },
          "dataLineageViz.overview.enabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Enable schema overview mode. When enabled and the graph exceeds `overview.threshold` nodes, the initial view shows schema-level bubbles instead of all objects. Set to `false` to always show the full object graph.",
            "scope": "resource"
          },
          "dataLineageViz.overview.threshold": {
            "type": "number",
            "default": 150,
            "minimum": 10,
            "maximum": 1000,
            "markdownDescription": "Node count above which schema overview mode auto-activates on initial load. Only applies when `overview.enabled` is `true`.",
            "scope": "resource"
          },
          "dataLineageViz.parseRulesFile": {
            "type": "string",
            "default": "",
            "markdownDescription": "Path to a custom parse rules YAML file. Leave empty for built-in defaults. [Customization guide](https://github.com/ChrisDevRepo/vscode_data_lineage/blob/main/docs/PARSE_RULES.md)",
            "scope": "resource"
          }
        }
      },
      {
        "title": "Database Connection",
        "properties": {
          "dataLineageViz.dmvQueryTimeout": {
            "type": "number",
            "default": 120,
            "minimum": 10,
            "maximum": 600,
            "markdownDescription": "Timeout (seconds) per DMV query. Increase for large databases.",
            "scope": "resource"
          },
          "dataLineageViz.dmvQueriesFile": {
            "type": "string",
            "default": "",
            "markdownDescription": "Path to a custom DMV queries YAML file. Leave empty for built-in defaults. [Customization guide](https://github.com/ChrisDevRepo/vscode_data_lineage/blob/main/docs/DMV_QUERIES.md)",
            "scope": "resource"
          }
        }
      },
      {
        "title": "Table Statistics",
        "properties": {
          "dataLineageViz.tableStatistics.enabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Show column statistics and row counts in the table design viewer (database import only).",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.standardModeEnabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Allow **Standard** profiling mode (adds AVG, STDEV, zero/empty counts). When `false`, only Quick mode is available — lighter queries.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.excludeExternalTables": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Skip profiling for external tables. External tables query remote data sources (S3, Blob, other databases) — profiling can be slow and expensive.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.queryTimeout": {
            "type": "number",
            "default": 60,
            "minimum": 10,
            "maximum": 600,
            "markdownDescription": "Timeout (seconds) per profiling query.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.sampleThreshold": {
            "type": "number",
            "default": 100000,
            "minimum": 0,
            "maximum": 999999999,
            "markdownDescription": "Advanced: Tables above this row count use sampling instead of a full scan. Set to `0` to always sample.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.sampleSize": {
            "type": "number",
            "default": 10000,
            "minimum": 100,
            "maximum": 1000000,
            "markdownDescription": "Advanced: Number of rows to sample when profiling large tables.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.useApproxDistinct": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Advanced: Use `APPROX_COUNT_DISTINCT` instead of exact `COUNT(DISTINCT)`. Much faster with ~2% margin. Requires SQL Server 2019+.",
            "scope": "resource"
          },
          "dataLineageViz.tableStatistics.maxColumns": {
            "type": "number",
            "default": 50,
            "minimum": 1,
            "maximum": 500,
            "markdownDescription": "Advanced: Maximum number of columns to profile per table. Columns beyond this cap are skipped. Prevents massive queries on wide tables.",
            "scope": "resource"
          }
        }
      },
      {
        "title": "Layout",
        "properties": {
          "dataLineageViz.layout.direction": {
            "type": "string",
            "enum": [
              "TB",
              "LR"
            ],
            "default": "LR",
            "enumDescriptions": [
              "Top to Bottom",
              "Left to Right (default)"
            ],
            "markdownDescription": "Direction of the dependency graph layout.",
            "scope": "resource"
          },
          "dataLineageViz.layout.edgeStyle": {
            "type": "string",
            "enum": [
              "default",
              "smoothstep",
              "step",
              "straight"
            ],
            "default": "default",
            "enumDescriptions": [
              "Smooth bezier curves",
              "Smooth step edges with rounded corners",
              "Step edges with sharp corners",
              "Straight lines"
            ],
            "markdownDescription": "Visual style for dependency edges.",
            "scope": "resource"
          },
          "dataLineageViz.layout.edgeAnimation": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Animate edges when running a trace.",
            "scope": "resource"
          },
          "dataLineageViz.layout.highlightAnimation": {
            "type": "boolean",
            "default": false,
            "markdownDescription": "Animate edges when clicking a node.",
            "scope": "resource"
          },
          "dataLineageViz.layout.minimapEnabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Show the graph minimap for navigation.",
            "scope": "resource"
          },
          "dataLineageViz.layout.rankSeparation": {
            "type": "number",
            "default": 120,
            "minimum": 20,
            "maximum": 300,
            "markdownDescription": "Advanced: Horizontal spacing between dependency layers (pixels).",
            "scope": "resource"
          },
          "dataLineageViz.layout.nodeSeparation": {
            "type": "number",
            "default": 30,
            "minimum": 10,
            "maximum": 200,
            "markdownDescription": "Advanced: Vertical spacing between nodes in the same layer (pixels).",
            "scope": "resource"
          }
        }
      },
      {
        "title": "Trace",
        "properties": {
          "dataLineageViz.trace.defaultUpstreamLevels": {
            "type": "number",
            "default": 3,
            "minimum": 0,
            "maximum": 99,
            "markdownDescription": "How many levels upstream (inputs) to trace by default.",
            "scope": "resource"
          },
          "dataLineageViz.trace.defaultDownstreamLevels": {
            "type": "number",
            "default": 3,
            "minimum": 0,
            "maximum": 99,
            "markdownDescription": "How many levels downstream (outputs) to trace by default.",
            "scope": "resource"
          }
        }
      },
      {
        "title": "Analysis",
        "properties": {
          "dataLineageViz.analysis.hubMinDegree": {
            "type": "number",
            "default": 8,
            "minimum": 1,
            "maximum": 50,
            "markdownDescription": "Minimum number of connections for an object to appear in the hub list.",
            "scope": "resource"
          },
          "dataLineageViz.analysis.islandMaxSize": {
            "type": "number",
            "default": 500,
            "minimum": 2,
            "maximum": 1000,
            "markdownDescription": "Max connected component size to qualify as an island. Higher values include larger disconnected subgraphs; set lower (e.g. 5) to focus on small isolated pairs/groups only.",
            "scope": "resource"
          },
          "dataLineageViz.analysis.longestPathMinNodes": {
            "type": "number",
            "default": 5,
            "minimum": 2,
            "maximum": 50,
            "markdownDescription": "Minimum number of objects in a dependency chain for longest-path analysis.",
            "scope": "resource"
          }
        }
      },
      {
        "title": "AI Assistant",
        "properties": {
          "dataLineageViz.ai.enabled": {
            "type": "boolean",
            "default": true,
            "description": "Enable the @lineage chat participant and AI lineage tools. Disable for users who do not use VS Code Copilot or want to remove AI features entirely."
          },
          "dataLineageViz.ai.maxRounds": {
            "type": "integer",
            "default": 50,
            "minimum": 5,
            "maximum": 100,
            "markdownDescription": "Maximum tool-call rounds per `@lineage` request. Increase for complex explorations on deep graphs. Each round is one AI reasoning step + tool call."
          },
          "dataLineageViz.ai.discoveryNodeCap": {
            "type": "integer",
            "default": 10,
            "minimum": 1,
            "maximum": 30,
            "markdownDescription": "Maximum scope nodes the AI may pull during a single discovery-phase catalog request before the engine forces an SM escalation via the gate. Discovery answers small ad-hoc questions in chat; over-budget requests are rejected with a hint to call `lineage_start_exploration`."
          },
          "dataLineageViz.ai.discoveryTokenBudget": {
            "type": "integer",
            "default": 10000,
            "minimum": 1000,
            "maximum": 32000,
            "markdownDescription": "Maximum estimated DDL tokens for a single discovery-phase catalog request. Exceeding either this or the node cap forces an SM escalation via the consent gate."
          },
          "dataLineageViz.ai.contextPayloadBudget": {
            "type": "integer",
            "default": 10000,
            "minimum": 1000,
            "maximum": 100000,
            "markdownDescription": "Token budget for `lineage_get_context` deciding whether to inline the full catalog or return a summary. Unrelated to the discovery scope budget above."
          },
          "dataLineageViz.ai.outputTemplateFile": {
            "type": "string",
            "default": "",
            "markdownDescription": "Path to custom AI output templates YAML. Controls how `@lineage` formats summary, description, badges, highlights, and notes. Leave empty for built-in defaults. Use Command Palette → *Create AI Output Templates* to scaffold. [Customization guide](https://github.com/ChrisDevRepo/vscode_data_lineage/blob/main/docs/AI_PROMPTS.md)"
          },
          "dataLineageViz.ai.showToolInvocations": {
            "type": "boolean",
            "default": false,
            "markdownDescription": "Show each `@lineage` tool call as an expandable chat part (with input JSON). When off (default), the copy button captures only the AI prose; tool progress is shown ephemerally via status text and full tool I/O is available in the **Data Lineage Viz** output channel. Turn on for developer debugging — note that VS Code's built-in chat copy will then include tool-call details."
          }
        }
      }
    ],
    "chatParticipants": [
      {
        "id": "dataLineageViz.lineage",
        "name": "lineage",
        "fullName": "Data Lineage Assistant",
        "description": "Ask questions about your loaded lineage graph — tables, procedures, dependencies, data flow.",
        "isSticky": true,
        "commands": [
          {
            "name": "trace",
            "description": "Trace data lineage for an object or column"
          },
          {
            "name": "search",
            "description": "Search for database objects by name or schema"
          }
        ],
        "disambiguation": [
          {
            "category": "lineage",
            "description": "Questions about database dependencies, data flow, stored procedures, table lineage, SQL object relationships, where data comes from or goes to",
            "examples": [
              "Where does revenue come from",
              "Trace the data flow for orders",
              "What tables does this procedure read from",
              "Show me the lineage for the sales schema",
              "What are the upstream sources of this table"
            ]
          }
        ]
      }
    ],
    "languageModelTools": [
      {
        "name": "lineage_get_context",
        "toolReferenceName": "lineage_get_context",
        "displayName": "Get Lineage Context",
        "userDescription": "Shows what is currently loaded and visible in the lineage graph.",
        "modelDescription": "Returns the current database context including loaded schemas, stats, and active UI filters.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage",
          "lineage-research"
        ],
        "icon": "$(info)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "lineage_search_objects",
        "toolReferenceName": "lineage_search_objects",
        "displayName": "Search Objects",
        "userDescription": "Search for database objects by name or column.",
        "modelDescription": "Search database objects by name or column name using substring or regex matching. Returns object IDs and metadata. Each result carries an `in_user_filter` flag — when an in-filter search returns 0 hits but out-of-filter results exist, include that schema in the next search; the active filter is a display preference, not a boundary.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage",
          "lineage-research"
        ],
        "icon": "$(search)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Object name or column name to search for (max 200 chars). Do not include schema prefixes like '[dbo].' here."
            },
            "types": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "table",
                  "view",
                  "procedure",
                  "function",
                  "external"
                ]
              },
              "description": "Include only these object types."
            },
            "schemas": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Restrict search to these schemas."
            },
            "mode": {
              "type": "string",
              "enum": [
                "substring",
                "regex"
              ],
              "description": "Search mode: 'substring' (default) or 'regex'."
            }
          },
          "required": [
            "query"
          ]
        }
      },
      {
        "name": "lineage_get_scope_bundle",
        "toolReferenceName": "lineage_get_scope_bundle",
        "displayName": "Get Scope Bundle",
        "userDescription": "Get a bounded BFS scope in one call, with optional DDL for all nodes in scope.",
        "modelDescription": "Discovery graph-scope retrieval. Use this for multi-object lineage questions when you need a bounded BFS scope in one call. For small scopes set include_ddl=true to fetch all scope DDL at once and answer directly in chat. If the tool returns over_discovery_budget, do not retry with per-node calls; call lineage_start_exploration so the user can review/approve the scope gate. Keep lineage_get_object_detail for single-object questions.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage",
          "lineage-research"
        ],
        "icon": "$(graph)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "origin": {
              "type": "string",
              "description": "Exact origin object ID (e.g. '[dbo].[FactSales]')."
            },
            "direction": {
              "type": "string",
              "enum": [
                "upstream",
                "downstream",
                "bidirectional"
              ],
              "description": "Traversal direction for scope expansion."
            },
            "depth": {
              "type": "number",
              "description": "Symmetric BFS depth. Use explicit finite values from user intent."
            },
            "upstream_depth": {
              "type": "number",
              "description": "Optional upstream depth override (bidirectional only)."
            },
            "downstream_depth": {
              "type": "number",
              "description": "Optional downstream depth override (bidirectional only)."
            },
            "include_ddl": {
              "type": "boolean",
              "description": "When true, include DDL for all scriptable nodes in scope and enforce discovery node/token budget guard."
            }
          },
          "required": [
            "origin"
          ]
        }
      },
      {
        "name": "lineage_get_object_detail",
        "toolReferenceName": "lineage_get_object_detail",
        "displayName": "Get Object Detail",
        "userDescription": "Get full details for a specific database object.",
        "modelDescription": "Primary single-object lookup for discovery and synthesis. Use this when the user asks about one specific object (DDL, columns, direct neighbors). For graph-scope lineage questions, prefer lineage_get_scope_bundle instead of chaining repeated per-node detail calls. During active SM exploration, use lineage_get_neighbor_columns instead.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage",
          "lineage-research"
        ],
        "icon": "$(symbol-class)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Exact object ID (e.g. '[dbo].[FactSales]')."
            }
          },
          "required": [
            "id"
          ]
        }
      },
      {
        "name": "lineage_get_neighbor_columns",
        "toolReferenceName": "lineage_get_neighbor_columns",
        "displayName": "Get Neighbor Columns",
        "userDescription": "Inspect a neighbor's columns for pruning decisions during active SM exploration.",
        "modelDescription": "Returns structural metadata (columns, types, nullability, foreign keys) for one or more direct neighbors of the current focus node. Use during active SM exploration to decide whether to prune a neighbor (e.g. when the focus DDL uses 'SELECT *' and you need the neighbor's column list). NEVER returns DDL bodies. Pass neighbor ids only — do NOT include the focus node id itself; neighbors must also be within the active BFS scope.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage"
        ],
        "icon": "$(symbol-field)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "ids": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "One or more direct-neighbor node IDs to inspect (e.g. ['[dbo].[FactSales]'])."
            }
          },
          "required": [
            "ids"
          ]
        }
      },
      {
        "name": "lineage_detect_graph_patterns",
        "toolReferenceName": "lineage_detect_graph_patterns",
        "displayName": "Detect Graph Patterns",
        "userDescription": "Find structural patterns like hubs, cycles, or orphans in the graph.",
        "modelDescription": "Analyzes the entire graph for specific structural patterns such as hubs, islands, orphans, longest paths, cycles, or external references.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage",
          "lineage-research"
        ],
        "icon": "$(graph)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "hubs",
                "islands",
                "orphans",
                "longest-path",
                "cycles",
                "external-refs"
              ],
              "description": "Type of pattern to detect."
            },
            "min_degree": {
              "type": "number",
              "description": "Hubs only: minimum degree."
            },
            "max_size": {
              "type": "number",
              "description": "Islands only: maximum component size."
            }
          },
          "required": [
            "type"
          ]
        }
      },
      {
        "name": "lineage_search_ddl",
        "toolReferenceName": "lineage_search_ddl",
        "displayName": "Search DDL",
        "userDescription": "Search SQL body scripts for a text pattern.",
        "modelDescription": "Performs a regex search across view, procedure, and function DDL bodies. Returns matching lines with context.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage",
          "lineage-research"
        ],
        "icon": "$(file-code)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Regex pattern to search for in DDL bodies."
            },
            "types": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "view",
                  "procedure",
                  "function"
                ]
              },
              "description": "Limit search to these object types."
            }
          },
          "required": [
            "query"
          ]
        }
      },
      {
        "name": "lineage_present_result",
        "toolReferenceName": "lineage_present_result",
        "displayName": "Present Result",
        "userDescription": "Generate a final report and visualization for the exploration.",
        "modelDescription": "Authors or updates the presentation layer for an exploration result. Use for report sections, node badges, notes, pruning, add_node_ids, and graph role/color highlights such as source/transform/target. Presentation-only changes must use this tool, not start_exploration.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "lineage-presentation"
        ],
        "icon": "$(sparkle)",
        "when": "dataLineageViz.modelLoaded",
        "inputSchema": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "maxLength": 60,
              "description": "Title for the report (max 60 chars)."
            },
            "summary": {
              "type": "string",
              "maxLength": 300,
              "description": "One-line digest for the info card."
            },
            "title": {
              "type": "string",
              "maxLength": 80,
              "description": "Optional. Heading for the description panel."
            },
            "intro": {
              "type": "string",
              "description": "Optional. Context paragraph shown before sections."
            },
            "closing": {
              "type": "string",
              "maxLength": 400,
              "description": "Optional. Closing note or risk assessment."
            },
            "prune_node_ids": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Nodes to remove from the final visualization."
            },
            "add_node_ids": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Nodes to add to the final visualization for presentation-only updates; pair with sections, notes, or highlight_groups as needed."
            },
            "layout_direction": {
              "type": "string",
              "enum": [
                "LR",
                "TB"
              ],
              "description": "Graph layout: Left-to-Right or Top-to-Bottom."
            },
            "highlight_groups": {
              "type": "array",
              "description": "Optional. Highlight groups for critical nodes.",
              "items": {
                "type": "object",
                "properties": {
                  "label": {
                    "type": "string"
                  },
                  "color": {
                    "type": "string",
                    "enum": [
                      "source",
                      "transform",
                      "target",
                      "good",
                      "warn",
                      "fail"
                    ]
                  },
                  "node_ids": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  }
                },
                "required": [
                  "label",
                  "color",
                  "node_ids"
                ]
              }
            },
            "sections": {
              "type": "array",
              "description": "Final graph/detail sections. Labels and node links are AI-authored; the engine validates and renders them.",
              "items": {
                "type": "object",
                "properties": {
                  "label": {
                    "type": "string",
                    "description": "Final short section/badge label, 2-3 words."
                  },
                  "angle": {
                    "type": "string",
                    "enum": [
                      "business",
                      "technical"
                    ],
                    "description": "Capture angle for this section group."
                  },
                  "node_ids": {
                    "type": "array",
                    "description": "Optional nodes linked to this section; omit nodes not discussed.",
                    "items": {
                      "type": "string"
                    }
                  },
                  "text": {
                    "type": "string",
                    "description": "Required detail body for this section label."
                  }
                },
                "required": [
                  "label",
                  "text"
                ]
              }
            },
            "notes": {
              "type": "array",
              "description": "Optional AI-authored captions shown below selected graph nodes; notes do not create badges or sections.",
              "items": {
                "type": "object",
                "properties": {
                  "node_id": {
                    "type": "string"
                  },
                  "text": {
                    "type": "string",
                    "description": "One-sentence node caption."
                  }
                },
                "required": [
                  "node_id",
                  "text"
                ]
              }
            },
            "is_update": {
              "type": "boolean",
              "description": "Set true to update an existing view."
            }
          },
          "required": [
            "name",
            "summary"
          ]
        }
      },
      {
        "name": "lineage_start_exploration",
        "toolReferenceName": "lineage_start_exploration",
        "when": "dataLineageViz.modelLoaded",
        "displayName": "Start Exploration",
        "userDescription": "Start an autonomous exploration of database objects for data flow, business rules, or investigations.",
        "modelDescription": "Starts an SM (Sliding Memory) exploration across database objects — hop-by-hop traversal with per-hop verdicts. The user must approve via the `confirm_sm_start` gate before any analysis runs. Use this when the user explicitly asks for graph/canvas visualization, requests column tracing (`targetColumns`), clicks deeper hop-by-hop analysis, or discovery returns `over_discovery_budget` for graph-scope DDL retrieval. Scope parameters: pick `direction`, `depth`, `depth_enforcement`, and `excludeTypes` to match how the user phrased scope. Honor NL filters: 'ignore UDFs/functions' → `excludeTypes:['function']`; 'ignore views' → `['view']`; 'only tables' → `['view','procedure','function']`. Depth values must come from user intent and current graph constraints: provide explicit finite numbers (no fixed defaults, no sentinel values). SM sessions lock scope at session start, and out-of-scope routes are deferred to a post-session review list rather than prompting mid-session. Compose `mission_brief` (3-6 sentences) capturing user intent, NL filters, name-based exclusions, and chosen scope — delivered verbatim every hop and surviving memory wipes. POST-SYNTHESIS SUPPLEMENT: in the follow-up phase, call with only `supplement:{nodeIds:[...]}` (no `origin` required) to extend the existing archive with new analysis for those ids. The prior engine, memory, and filter state are reused — new slots merge into the archive via the SM hop loop; a fresh re-render via `lineage_present_result` follows. Use this path for deferred-question continuations and user-requested 'add node X' refinements.\n\n## Decision order\n\nWhen the user refines a pending `confirm_sm_start` gate, settle decisions in this order — later axes depend on earlier ones being fixed:\n\n1. **Origin** — locked. Refine cannot change origin; that is a fresh question. If the user names a different starting object, ask them to cancel and re-ask.\n2. **Direction** (`upstream` / `downstream` / `bidirectional`) — refinable. Re-runs BFS.\n3. **Depth** (`depth`, `depth_enforcement`, plus `upstream_depth` / `downstream_depth` for asymmetric bidirectional) — refinable. Re-runs BFS.\n4. **Per-node bucket** for each node BFS surfaced — analyze (default) / pass-through / prune.\n5. **Classification + tracing** — `classification`, `targetColumns`.\n\n## Scope filters\n\n`excludeSchemas`, `excludeTypes`, `excludeNodeIds`, and `passNodeIds` are applied at BFS scope-build time. Each call REPLACES the prior filter set on the engine — accumulate across rounds by sending every prior filter plus the new one in a single call.\n\n## Pass vs prune\n\nA node has two narrowing semantics. Choose deliberately:\n\n- **`passNodeIds`** — node stays in scope; BFS still routes through it (descendants reachable); the engine auto-emits `verdict:'pass'` so no analysis is written for that node. Topology preserved. Default interpretation when the user says 'ignore', 'skip', or 'don't analyze X'.\n- **`excludeNodeIds`** / **`excludeSchemas`** / **`excludeTypes`** — node (and its subtree reachable only through it) is cut at BFS time. Use only when the user explicitly says 'remove', 'drop', 'prune', 'cut', or names a leaf they reject.\n\nIf you propose pruning a node that would orphan in-scope descendants, rewrite the call as `passNodeIds` for those ids unless the user has explicitly approved removing the subtree.\n\n## Gate refinement loop\n\nWhen a `confirm_sm_start` gate is pending and the user wants to adjust the proposed scope:\n\n1. Read the user's chat message and map their intent to the right axis using the decision order above. Phrasing varies — they may say ignore / drop / remove / without / no / skip — and they may name schemas, object types, or specific objects. Use your judgment; if intent is unclear, ask one short clarifying question and call no tool that turn.\n2. Call `lineage_start_exploration` with the same `origin` from the prior call, plus the updated `direction` / `depth` / `upstream_depth` / `downstream_depth` / `excludeTypes` / `excludeSchemas` / `excludeNodeIds` / `passNodeIds` / `classification` / `targetColumns` as the user requested.\n3. The engine re-runs BFS and re-emits the gate with the new counts. The user reviews and either approves, refines further, or cancels.\n4. During this loop, only `lineage_start_exploration` is appropriate — `submit_findings` is not yet available, and analysis begins only after the user approves.\n\n<examples>\n<example>\nUser: 'ignore the staging schema'\nCall: lineage_start_exploration({ origin: <kept>, depth: <kept>, excludeSchemas: ['staging'] })\n</example>\n\n<example>\nPrior call excluded 'staging'. User: 'also drop procedures.'\nCall: lineage_start_exploration({ origin: <kept>, depth: <kept>, excludeSchemas: ['staging'], excludeTypes: ['procedure'] })\n</example>\n\n<example>\nUser: 'ignore usp_PostJournal but I still want to see what it reads from'\nCall: lineage_start_exploration({ origin: <kept>, depth: <kept>, passNodeIds: ['[dbo].[usp_PostJournal]'] })\n</example>\n\n<example>\nUser: 'remove the staging subtree entirely — I don\\'t need anything below it'\nCall: lineage_start_exploration({ origin: <kept>, depth: <kept>, excludeSchemas: ['staging'] })\n</example>\n\n<example>\nUser: 'all upstream, one downstream, and trace TotalRevenue'\nCall: lineage_start_exploration({ origin: <kept>, direction: 'bidirectional', upstream_depth: <computed_upstream_depth>, downstream_depth: 1, targetColumns: ['TotalRevenue'] })\n</example>\n</examples>",
        "tags": [
          "lineage",
          "lineage-engine"
        ],
        "inputSchema": {
          "type": "object",
          "properties": {
            "question": {
              "type": "string",
              "description": "The user's exploration question."
            },
            "origin": {
              "type": "string",
              "description": "Starting node ID (e.g. '[dbo].[FactSales]')."
            },
            "targetColumns": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Optional. Specific column names to trace (activates Column Trace mode)."
            },
            "direction": {
              "type": "string",
              "enum": [
                "upstream",
                "downstream",
                "bidirectional"
              ],
              "description": "BFS direction for initial scope discovery."
            },
            "depth": {
              "type": "number",
              "description": "Symmetric BFS depth from origin (default 5). Overridden per-direction by upstream_depth / downstream_depth when those are provided."
            },
            "upstream_depth": {
              "type": "number",
              "description": "Optional. BFS depth for upstream (inbound/input) traversal. Overrides 'depth' for the upstream direction when direction='bidirectional'. Use when the user requests asymmetric levels, e.g. 'all upstream, 2 downstream'."
            },
            "downstream_depth": {
              "type": "number",
              "description": "Optional. BFS depth for downstream (outbound/output) traversal. Overrides 'depth' for the downstream direction when direction='bidirectional'. Use when the user requests asymmetric levels."
            },
            "depth_enforcement": {
              "type": "string",
              "enum": [
                "strict",
                "soft",
                "silent"
              ],
              "description": "How to treat the depth budget: 'strict' (hard cap), 'soft' (+1 depth expansion allowed), or 'silent' (+2 depth allowed). Default 'silent'."
            },
            "schemas": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Optional. Override active filter to search specific schemas."
            },
            "mission_brief": {
              "type": "string",
              "maxLength": 1500,
              "description": "A 3-6 sentence natural-language briefing distilling user intent, NL filters, scope choices, and pruning criteria."
            },
            "excludeTypes": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "table",
                  "view",
                  "procedure",
                  "function",
                  "external"
                ]
              },
              "description": "Optional. Object types to prune from the exploration scope. Use whenever the user names a type-level filter in NL — e.g. 'no views' / 'exclude all views' / 'tables only' / 'skip UDFs' map to ['view'] / ['view'] / ['view','procedure','function'] / ['function'] respectively. ALSO honor the GUI filter as the user's default: when `filter_context.active_types` from a prior search result lists e.g. ['table','procedure'], the user has already hidden ['view','function'] in the GUI — include those in `excludeTypes` unless the user's chat explicitly asks to broaden ('include views', 'also look at UDFs'). The gate shows the resulting scope; if the user wants to widen, they'll say so on refine. REPLACE semantics — each call overrides prior. Cumulative across refine rounds (re-send all prior plus the new)."
            },
            "excludeSchemas": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Optional. Schemas to prune from the exploration scope (case-insensitive). REPLACE semantics. Use when the user says 'remove/drop schema X' or 'ignore X-schema objects'. ALSO honor `filter_context.active_schemas` from a prior search result as the user's GUI default: any schema present in the loaded model but missing from `active_schemas` is currently hidden — include it here unless the user's chat explicitly asks to broaden ('also look at STAGING')."
            },
            "excludeNodeIds": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Optional. Specific node ids to prune from the BFS scope (case-insensitive). Cuts the node and any subtree reachable only through it. Use when the user says 'remove/drop/cut object X'. REPLACE semantics."
            },
            "passNodeIds": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Optional. Node ids to keep in scope but auto-pass — engine emits verdict:'pass' so no analysis is written, topology preserved so descendants stay reachable. Default interpretation when the user says 'ignore/skip/don't analyze X'. REPLACE semantics."
            },
            "classification": {
              "type": "string",
              "enum": [
                "business",
                "technical",
                "both"
              ],
              "description": "REQUIRED. Declare the analysis angle: `business`, `technical`, or `both`. Pick `technical` only when the user explicitly asks about performance, execution plans, indexes, query tuning, statistics, or data-quality / null-handling semantics. Pick `both` only when the user explicitly asks for both angles in one ask. For everything else — lineage / origin / impact / 'where does X come from' / 'what reads from Y' / column traces — pick `business`. When the user's intent is ambiguous, weight toward `business` over `technical`. The engine rejects missing or invalid values; there is no fallback default."
            },
            "supplement": {
              "type": "object",
              "description": "Post-synthesis supplement. Supply only in the follow-up phase to extend the existing archive with additional ids. When present, `origin` may be omitted. Runs through the SM hop loop; new slots merge into the existing archive.",
              "properties": {
                "nodeIds": {
                  "type": "array",
                  "items": {
                    "type": "string",
                    "minLength": 1
                  },
                  "minItems": 1,
                  "description": "Node ids to add to the existing exploration (typically picked from `deferred_questions`)."
                }
              },
              "required": [
                "nodeIds"
              ]
            }
          },
          "required": [
            "question",
            "classification"
          ]
        }
      },
      {
        "name": "lineage_submit_findings",
        "toolReferenceName": "lineage_submit_findings",
        "when": "dataLineageViz.modelLoaded",
        "displayName": "Submit Findings",
        "userDescription": "Submit analysis of the current node and propose next routes in the exploration.",
        "modelDescription": "Submits analysis for the current focus node and requests the next hop's data. The `sections` array carries one captured section per fired *_capture template — `business` classification → 1 business section; `technical` → 1 technical; `both` → 2 sections (one of each). Dispatch is mode-specific: BB accepts prune semantics, CT requires `column_flow` and forbids BB prune fields.",
        "tags": [
          "lineage",
          "lineage-engine"
        ],
        "inputSchema": {
          "type": "object",
          "properties": {
            "focus_node_id": {
              "type": "string",
              "description": "ID of the current focus node."
            },
            "sections": {
              "type": "array",
              "maxItems": 2,
              "items": {
                "type": "object",
                "properties": {
                  "angle": {
                    "type": "string",
                    "enum": [
                      "business",
                      "technical"
                    ],
                    "description": "Which capture template produced this section."
                  },
                  "text": {
                    "type": "string",
                    "description": "Pre-formatted section body — written per the angle's *_capture instruction. Lifted verbatim by synthesis."
                  }
                },
                "required": [
                  "angle",
                  "text"
                ]
              },
              "description": "One section per fired *_capture template. Length 1 (business OR technical classification) or 2 (both). May be empty for verdict=prune."
            },
            "summary": {
              "type": "string",
              "description": "One short sentence digest of the whole node — echoed in future hops via short_term_memory."
            },
            "verdict": {
              "type": "string",
              "enum": [
                "analyze",
                "pass",
                "prune"
              ],
              "description": "Action for focus node: 'analyze' (store findings), 'pass' (mark visited, wire-only), or 'prune' (BB mode only). CT mode accepts only analyze/pass and rejects prune with ct_verdict_forbidden."
            },
            "route_requests": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "nodeId": {
                    "type": "string",
                    "description": "Exact, schema-qualified neighbor ID to visit (e.g. `[schema].[name]`). Must come VERBATIM from a prior tool result's `next_hop` / `neighbors[]` data, a `lineage_get_neighbor_columns` lookup, or a `lineage_search_objects` result. Inventing or guessing an id (even a small typo) causes a `route_validation_failed` rejection — the rejection envelope returns up to 3 fuzzy candidates per unresolved id; reuse the matching candidate verbatim, or call `lineage_search_objects` with the un-bracketed name to find the real id."
                  },
                  "question": {
                    "type": "string",
                    "description": "Sub-question for the next hop."
                  },
                  "columns": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "Specific columns to follow in next hop."
                  }
                },
                "required": [
                  "nodeId",
                  "question"
                ]
              },
              "description": "Current-hop neighbor route requests. Use concrete verification sub-questions; out-of-scope mission-relevant routes are deferred for post-synthesis follow-up."
            },
            "prune_neighbors": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Current-hop neighbor IDs to prune from exploration (BB only). CT mode rejects this field with bb_field_forbidden_in_ct."
            },
            "column_flow": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "out_col": {
                    "type": "string",
                    "description": "Tracked output column on the current focus node."
                  },
                  "writes_to": {
                    "type": "object",
                    "properties": {
                      "node": { "type": "string" },
                      "col": { "type": "string" }
                    },
                    "required": ["node", "col"]
                  },
                  "contributors": {
                    "type": "array",
                    "items": {
                      "type": "object",
                      "properties": {
                        "from_node": { "type": "string" },
                        "from_col": { "type": "string" },
                        "role": {
                          "type": "string",
                          "enum": ["formula", "rename", "case", "coalesce", "join_value", "aggregate", "filter_only", "source"]
                        }
                      },
                      "required": ["from_node", "from_col", "role"]
                    }
                  }
                },
                "required": ["out_col", "contributors"]
              },
              "description": "CT mode only. Required in CT for every hop (`[]` means no tracked interaction). BB mode rejects this field with bb_field_unknown."
            },
            "badge_label": {
              "type": "string",
              "maxLength": 50,
              "description": "Optional hop helper label; advisory only, not rendered directly."
            },
            "note_caption": {
              "type": "string",
              "maxLength": 200,
              "description": "Optional AI-authored one-sentence preview for this node."
            }
          },
          "required": [
            "focus_node_id",
            "sections",
            "summary",
            "verdict"
          ]
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run build:ext && npm run build:webview",
    "build": "npm run build:ext && npm run build:webview",
    "build:ext": "node esbuild.config.mjs",
    "build:webview": "vite build",
    "dev": "vite",
    "watch": "npm run watch:ext",
    "watch:ext": "node esbuild.config.mjs --watch",
    "test": "tsx tests/unit/dacpacExtractor.test.ts && tsx tests/unit/graphBuilder.test.ts && tsx tests/unit/parser-edge-cases.test.ts && tsx tests/unit/graphAnalysis.test.ts && tsx tests/unit/graph-analysis-aw.test.ts && tsx tests/unit/dmvExtractor.test.ts && tsx tests/unit/tsql-complex.test.ts && tsx tests/unit/projectStore.test.ts && tsx tests/unit/snapshot-aw-baseline.ts && tsx tests/unit/ai-tool-registration.test.ts && tsx tests/unit/schemaColors.test.ts && tsx tests/unit/repeat-reject-guard.test.ts && tsx tests/unit/classification.test.ts && tsx tests/unit/start-exploration-schema.test.ts && tsx tests/unit/chatResponseWriter.test.ts && tsx tests/unit/messageEnvelope.test.ts && tsx tests/unit/refine-loop.test.ts && tsx tests/unit/followup-confirmation.test.ts && tsx tests/unit/transient-retry.test.ts && tsx tests/unit/column-flow-validation.test.ts && tsx tests/unit/submit-findings-schema.test.ts && tsx tests/unit/toolPolicy.test.ts && tsx tests/unit/prompt-composition.test.ts && tsx tests/unit/navigation-engine.test.ts && tsx tests/unit/navigation-engine-cascade.test.ts && tsx tests/unit/navigation-engine-bipartite.test.ts && tsx tests/unit/navigation-engine-supplement.test.ts && tsx tests/unit/navigation-engine-synthesis-regression.test.ts && tsx tests/unit/present-result-closure.test.ts && tsx tests/unit/log-normalization.test.ts && tsx tests/unit/ai-preview-rendering.test.ts",
    "test:unit": "npm run test",
    "test:parser": "tsx tests/unit/parser-edge-cases.test.ts && tsx tests/unit/tsql-complex.test.ts",
    "test:graph": "tsx tests/unit/graphBuilder.test.ts && tsx tests/unit/graphAnalysis.test.ts",
    "test:baseline": "tsx tests/unit/snapshot-aw-baseline.ts && tsx tests/unit/graph-analysis-aw.test.ts",
    "test:hooks": "vitest run --config vitest.config.ts",
    "test:snapshot": "tsx tests/unit/snapshot-aw-baseline.ts",
    "test:snapshot:update": "tsx tests/unit/snapshot-aw-baseline.ts --update",
    "package": "npx @vscode/vsce package"
  },
  "dependencies": {
    "@dagrejs/dagre": "^2.0.4",
    "@floating-ui/react": "^0.27.19",
    "@monaco-editor/react": "^4.7.0",
    "@xyflow/react": "^12.4.0",
    "fast-xml-parser": "^5.7.1",
    "graphology": "^0.25.4",
    "graphology-components": "^1.5.4",
    "graphology-shortest-path": "^2.1.0",
    "graphology-traversal": "^0.3.1",
    "graphology-types": "^0.24.8",
    "js-yaml": "^4.1.0",
    "jszip": "^3.10.1",
      "katex": "^0.16.46",
    "monaco-editor": "^0.53.0",
      "react": "^19.0.0",
      "react-dom": "^19.0.0",
      "react-markdown": "^10.1.0",
      "rehype-katex": "^7.0.1",
      "remark-math": "^6.0.0",
      "remark-gfm": "^4.0.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@types/js-yaml": "^4.0.9",
    "@types/jsdom": "^28.0.1",
    "@types/node": "^20.11.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@types/vscode": "^1.95.0",
    "@vitejs/plugin-react": "^4.3.0",
    "autoprefixer": "^10.4.20",
    "esbuild": "^0.25.0",
    "jsdom": "^29.0.1",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vite": "^6.4.2",
    "vitest": "^3.2.4"
  },
  "overrides": {
    "fast-xml-builder": "^1.1.7",
    "dompurify": "^3.4.1",
    "serialize-javascript": "^7.0.5",
    "picomatch": "4.0.4"
  }
}
